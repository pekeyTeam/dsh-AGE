window.__ModuleLoader__.load({
	id: "dsh-age",
	factory: (require) => {
		var module = { exports: {} };
		var exports = module.exports;
		Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
		let react = require("react");
		let react_jsx_runtime = require("react/jsx-runtime");
		/** Width divided by height, so callers can size from one dimension. */
		const MARK_ASPECT = 512 / 209;
		/** The mark as a data URI, for use as a CSS mask. */
		const MARK_DATA_URI = "data:image/png;base64,";
		//#endregion
		//#region src/client/hud/mark.ts
		/** The brand blue, sampled from the source artwork's flat field. */
		const AGE_BLUE = "#1a6ed0";
		/** The wordmark's white, sampled from the source artwork. */
		const AGE_WHITE = "#fdfffe";
		/** The glow drawn behind the badge while a scan is running. */
		const AGE_GLOW = "rgba(26, 110, 208, 0.55)";
		/**
		* Alert colours for the badge plate.
		*
		* The mark itself stays white in every state — that is how the source artwork is
		* built, and recolouring the letters would make the badge stop reading as the
		* brand. Only the field behind them changes.
		*/
		const AGE_SCAN_AMBER = "#c98a00";
		/** The plate colour while the main thread is blocked. */
		const AGE_HITCH_RED = "#c5342a";
		//#endregion
		//#region src/client/hud/AgeMark.tsx
		/** How much of the plate's width is padding around the letterforms. */
		const PLATE_PADDING_RATIO = .22;
		/** Corner rounding of the plate, as a fraction of its height. */
		const PLATE_RADIUS_RATIO = .28;
		/**
		* The AGE wordmark.
		*
		* @param props - see {@link AgeMarkProps}.
		* @returns the mark, optionally on its plate.
		*/
		function AgeMark({ width, color = AGE_WHITE, plate = AGE_BLUE, title }) {
			const height = width / MARK_ASPECT;
			const mask = {
				display: "block",
				width,
				height,
				backgroundColor: color,
				maskImage: `url("${MARK_DATA_URI}")`,
				WebkitMaskImage: `url("${MARK_DATA_URI}")`,
				maskSize: "contain",
				WebkitMaskSize: "contain",
				maskRepeat: "no-repeat",
				WebkitMaskRepeat: "no-repeat",
				maskPosition: "center",
				WebkitMaskPosition: "center"
			};
			if (plate === null) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				role: title === void 0 ? "presentation" : "img",
				"aria-label": title,
				style: mask
			});
			const padding = height * PLATE_PADDING_RATIO;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				role: title === void 0 ? "presentation" : "img",
				"aria-label": title,
				style: {
					display: "inline-flex",
					alignItems: "center",
					justifyContent: "center",
					padding,
					borderRadius: (height + padding * 2) * PLATE_RADIUS_RATIO,
					backgroundColor: plate,
					flex: "none"
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { style: mask })
			});
		}
		//#endregion
		//#region src/client/persist.ts
		/** Where the engine's settings are kept. */
		const STORAGE_PREFIX = "dsh-age:";
		/**
		* Storage is not always writable — private-mode Safari, sandboxed frames, a
		* jsdom run — and a jank plugin must never be the reason a session fails to
		* render. Every access is guarded, and a failed write just means the preference
		* does not outlive the tab.
		*
		* @param key - suffix of the storage key.
		* @returns the parsed value, or `undefined` when absent or unreadable.
		*/
		function readStored(key) {
			try {
				const raw = localStorage.getItem(`${STORAGE_PREFIX}${key}`);
				return raw === null ? void 0 : JSON.parse(raw);
			} catch {
				return;
			}
		}
		/**
		* Best-effort write.
		*
		* @param key - suffix of the storage key.
		* @param value - value to serialise.
		* @returns true when the write landed.
		*/
		function writeStored(key, value) {
			try {
				localStorage.setItem(`${STORAGE_PREFIX}${key}`, JSON.stringify(value));
				return true;
			} catch {
				return false;
			}
		}
		/** Remove one stored key. */
		function removeStored(key) {
			try {
				localStorage.removeItem(`${STORAGE_PREFIX}${key}`);
			} catch {}
		}
		/**
		* Build a store over one stored key.
		*
		* The live value is held in memory and storage is the backing copy, not the
		* source of truth: reading through `localStorage` on every access would make
		* each `get()` a parse, and would silently revert a setting the instant storage
		* refused a write.
		*
		* @param key - suffix of the storage key.
		* @param defaults - the complete default value.
		* @param merge - how to combine a stored partial with the defaults; the default
		* shallow-merges, which is what keeps an older stored object loadable after a
		* field is added.
		* @returns the store.
		*/
		function createStore(key, defaults, merge = (stored, base) => ({
			...base,
			...stored
		})) {
			let value = merge(readStored(key) ?? {}, defaults);
			const listeners = /* @__PURE__ */ new Set();
			const notify = () => {
				for (const listener of listeners) listener();
			};
			return {
				get: () => value,
				set: (patch) => {
					value = {
						...value,
						...patch
					};
					writeStored(key, value);
					notify();
				},
				reset: () => {
					value = { ...defaults };
					removeStored(key);
					notify();
				},
				subscribe: (listener) => {
					listeners.add(listener);
					return () => listeners.delete(listener);
				}
			};
		}
		//#endregion
		//#region src/client/presets.ts
		/** The intensity table, mildest first. */
		const PRESETS = {
			light: {
				id: "light",
				scanGapMs: [3e4, 6e4],
				scanWindowMs: [25e3, 5e4],
				burstsPerScan: [14, 28],
				hitchMs: [200, 500],
				refractoryMs: 900,
				budgetMsPerMinute: 6e3,
				kinds: ["mouse", "stream-chunk"],
				streamFreezeMs: [300, 700],
				chunkFrameBlockMs: 50,
				chunkFrames: [2, 5],
				stretchScale: 1.008,
				stretchMode: "letter-spacing",
				inputLagMs: [80, 180],
				interactionStallMs: [120, 260],
				startupHeavyScan: false,
				pauseAnimationsDuringHitch: false
			},
			medium: {
				id: "medium",
				scanGapMs: [15e3, 35e3],
				scanWindowMs: [35e3, 65e3],
				burstsPerScan: [14, 28],
				hitchMs: [400, 1e3],
				refractoryMs: 1200,
				budgetMsPerMinute: 1e4,
				kinds: [
					"mouse",
					"stream-stall",
					"stream-chunk",
					"interaction"
				],
				streamFreezeMs: [600, 1500],
				chunkFrameBlockMs: 60,
				chunkFrames: [3, 9],
				stretchScale: 1.015,
				stretchMode: "letter-spacing",
				inputLagMs: [150, 400],
				interactionStallMs: [250, 500],
				startupHeavyScan: false,
				pauseAnimationsDuringHitch: false
			},
			heavy: {
				id: "heavy",
				scanGapMs: [5e3, 15e3],
				scanWindowMs: [3e4, 7e4],
				burstsPerScan: [4, 10],
				hitchMs: [1e3, 3e3],
				refractoryMs: 4e3,
				budgetMsPerMinute: 15e3,
				kinds: [
					"mouse",
					"stream-stall",
					"stream-chunk",
					"stream-stretch",
					"interaction"
				],
				streamFreezeMs: [1200, 3e3],
				chunkFrameBlockMs: 70,
				chunkFrames: [4, 14],
				stretchScale: 1.03,
				stretchMode: "transform",
				inputLagMs: [250, 700],
				interactionStallMs: [400, 900],
				startupHeavyScan: true,
				pauseAnimationsDuringHitch: true
			},
			hell: {
				id: "hell",
				scanGapMs: [1e3, 4e3],
				scanWindowMs: [4e4, 9e4],
				burstsPerScan: [10, 24],
				hitchMs: [1500, 2500],
				refractoryMs: 1200,
				budgetMsPerMinute: 24e3,
				kinds: [
					"mouse",
					"stream-stall",
					"stream-chunk",
					"stream-stretch",
					"interaction"
				],
				streamFreezeMs: [2e3, 4500],
				chunkFrameBlockMs: 90,
				chunkFrames: [6, 24],
				stretchScale: 1.05,
				stretchMode: "transform",
				inputLagMs: [500, 1200],
				interactionStallMs: [700, 1500],
				startupHeavyScan: true,
				pauseAnimationsDuringHitch: true
			}
		};
		/** Presets in ascending intensity, for the settings page. */
		const PRESET_ORDER = [
			"light",
			"medium",
			"heavy",
			"hell"
		];
		/** The preset the plugin ships with: the specification, not the compromise. */
		const DEFAULT_PRESET_ID = "heavy";
		/**
		* Resolve a persisted id back to a preset.
		*
		* Unknown ids fall back to the default rather than throwing: a stored value
		* can survive an upgrade that renames or drops a preset, and a jank plugin that
		* fails to boot because of a stale localStorage string would be a genuine bug
		* in the only code path that is supposed to be safe.
		*
		* @param id - possibly-unknown preset identifier.
		* @returns a preset, always.
		*/
		function presetOf(id) {
			return PRESETS[id] ?? PRESETS["heavy"];
		}
		/** Storage key suffix for the settings object. */
		const SETTINGS_KEY = "settings";
		/**
		* A fresh seed.
		*
		* Only ever used on a first run: the value is written back immediately, so the
		* session stays reproducible even before the user touches anything.
		*
		* @returns a seed string.
		*/
		function freshSeed() {
			return `age-${Date.now().toString(36)}`;
		}
		/** Build the defaults, given a seed to use. */
		function defaultsWith(seed) {
			return {
				schema: 2,
				enabled: true,
				presetId: DEFAULT_PRESET_ID,
				correlate: true,
				mouse: true,
				text: true,
				interaction: true,
				mouseMode: "cursor",
				hud: true,
				hudCorner: "top-right",
				hudCollapsed: false,
				toastChance: 35,
				seed
			};
		}
		/**
		* Bring a stored object forward to the current schema.
		*
		* Two migrations so far, and both exist because a stored value can outlive the
		* reason it had that value:
		*
		* - **v0 → v1.** `mouseMode: 'hitch'` was written when that was the only option,
		*   long before the pointer had a mechanism of its own. Left in place it would
		*   pin exactly the users who had the plugin installed longest to the setting
		*   that does nothing to the pointer — a bug that presents as a missing feature.
		* - **v1 → v2.** Three switches for what turned out to be one symptom. Collapsed
		*   with "any one on wins" rather than "all must be on": a user who had switched
		*   two off and left one on was asking for text effects, and the strict reading
		*   would silently turn them all off instead.
		*
		* @param stored - whatever was in storage.
		* @param base - the defaults, already carrying the resolved seed.
		* @returns settings that match the current schema.
		*/
		function migrate(stored, base) {
			const from = typeof stored.schema === "number" ? stored.schema : 0;
			if (from >= 2) return {
				...base,
				...stored
			};
			const { streamStall, streamChunk, streamStretch, ...rest } = stored;
			const text = streamStall !== void 0 || streamChunk !== void 0 || streamStretch !== void 0 ? streamStall === true || streamChunk === true || streamStretch === true : base.text;
			return {
				...base,
				...rest,
				schema: 2,
				text,
				mouseMode: from >= 1 ? rest.mouseMode ?? base.mouseMode : base.mouseMode
			};
		}
		/**
		* Create the settings store.
		*
		* A first run persists its generated seed straight away rather than leaving it
		* as a default, because a default that is never written would be regenerated on
		* every reload — and a schedule you cannot reproduce across a refresh is not
		* reproducible at all. The same write is what stamps the schema version onto a
		* first-run object, so a later upgrade can tell an untouched default from a
		* deliberate choice.
		*
		* @returns the store.
		*/
		function createAgeSettingsStore() {
			const stored = readStored(SETTINGS_KEY);
			const storedSeed = stored?.seed;
			const seed = typeof storedSeed === "string" && storedSeed !== "" ? storedSeed : freshSeed();
			const store = createStore(SETTINGS_KEY, defaultsWith(seed), migrate);
			if (stored?.schema !== 2) writeStored(SETTINGS_KEY, store.get());
			return store;
		}
		/**
		* The settings, as a React value that re-renders on every change.
		*
		* @param store - the engine's store.
		* @returns the current settings.
		*/
		function useAgeSettings(store) {
			return (0, react.useSyncExternalStore)(store.subscribe, store.get, store.get);
		}
		/**
		* Which symptoms are active, as a set the engine can intersect with a preset.
		*
		* @param settings - current settings.
		* @returns the enabled symptoms, in a stable order.
		*/
		function enabledSymptoms(settings) {
			const symptoms = [];
			if (settings.mouse) symptoms.push("mouse");
			if (settings.text) symptoms.push("stream-stall", "stream-chunk", "stream-stretch");
			if (settings.interaction) symptoms.push("interaction");
			return symptoms;
		}
		/**
		* The preset to run, with the user's symptom switches applied.
		*
		* The preset decides *how hard*; the switches decide *whether at all*. Keeping
		* those separate is what lets a user silence the text symptoms without also
		* softening the pointer, which a preset-only model cannot express.
		*
		* @param settings - current settings.
		* @returns a preset whose `kinds` is the intersection.
		*/
		function effectivePreset(settings) {
			const preset = presetOf(settings.presetId);
			const enabled = new Set(enabledSymptoms(settings));
			return {
				...preset,
				kinds: preset.kinds.filter((kind) => enabled.has(kind))
			};
		}
		/** Human-readable preset labels, keyed by id, in both shipped languages. */
		const PRESET_LABEL = {
			light: {
				zh: "轻度",
				en: "Light"
			},
			medium: {
				zh: "中等",
				en: "Medium"
			},
			heavy: {
				zh: "重度",
				en: "Heavy"
			},
			hell: {
				zh: "地狱",
				en: "Hell"
			}
		};
		/** One-line preset descriptions, in both shipped languages. */
		const PRESET_BLURB = {
			light: {
				zh: "每 30–60 秒扫一次，单次卡 0.2–0.5 秒。偶尔卡一下，几乎不影响干活。",
				en: "A scan every 30–60s, 0.2–0.5s each. A occasional stumble you can ignore."
			},
			medium: {
				zh: "每 15–35 秒扫一次，单次卡 0.4–1 秒。能感觉到，但还忍得住。",
				en: "A scan every 15–35s, 0.4–1s each. Noticeable, still bearable."
			},
			heavy: {
				zh: "每 5–15 秒扫一次，单次卡 1–3 秒。环境稍加恶劣。",
				en: "A scan every 5–15s, 1–3s each. A slightly harsher environment."
			},
			hell: {
				zh: "每 2–6 秒扫一次，单次 1.5–2.5 秒，最高频率。",
				en: "A scan every 2–6s, 1.5–2.5s each. Maximum frequency."
			}
		};
		//#endregion
		//#region src/client/locale.ts
		/**
		* Map the host's locale tag onto one of ours.
		*
		* @param active - the host's active locale, e.g. `zh-CN`.
		* @returns `zh` for anything Chinese, `en` otherwise.
		*/
		function ageLocale(active) {
			return active.toLowerCase().startsWith("zh") ? "zh" : "en";
		}
		/**
		* Pick the string for the active language.
		*
		* @param locale - active language.
		* @param copy - the two variants.
		* @returns the matching string.
		*/
		function pick(locale, copy) {
			return copy[locale];
		}
		//#endregion
		//#region src/client/AgeSection.tsx
		const card = {
			border: "1px solid rgba(128, 128, 128, 0.28)",
			borderRadius: 12,
			padding: "14px 16px",
			marginBottom: 16
		};
		const heading = {
			fontSize: 13,
			fontWeight: 600,
			marginBottom: 10,
			opacity: .9
		};
		const hint = {
			fontSize: 12,
			lineHeight: 1.55,
			opacity: .68
		};
		const rule = {
			border: 0,
			borderTop: "1px solid rgba(128, 128, 128, 0.28)",
			margin: "18px 0"
		};
		/** One labelled switch. */
		function Toggle(props) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
				style: {
					display: "flex",
					alignItems: "flex-start",
					gap: 10,
					padding: "7px 0",
					cursor: props.disabled === true ? "not-allowed" : "pointer",
					opacity: props.disabled === true ? .45 : 1
				},
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
					type: "checkbox",
					checked: props.checked,
					disabled: props.disabled,
					onChange: (event) => props.onChange(event.target.checked),
					style: {
						marginTop: 2,
						accentColor: AGE_BLUE
					}
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
					style: {
						display: "flex",
						flexDirection: "column",
						gap: 2
					},
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						style: { fontSize: 13 },
						children: props.label
					}), props.description !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						style: hint,
						children: props.description
					})]
				})]
			});
		}
		/** One labelled choice set. */
		function Choice(props) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				style: { padding: "7px 0" },
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: {
							fontSize: 13,
							marginBottom: 6
						},
						children: props.label
					}),
					props.description !== void 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: {
							...hint,
							marginBottom: 8
						},
						children: props.description
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: {
							display: "flex",
							gap: 6,
							flexWrap: "wrap"
						},
						children: props.options.map((option) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
							type: "button",
							onClick: () => props.onChange(option.id),
							style: {
								padding: "5px 12px",
								borderRadius: 999,
								fontSize: 12,
								cursor: "pointer",
								border: `1px solid ${option.id === props.value ? AGE_BLUE : "rgba(128,128,128,0.35)"}`,
								background: option.id === props.value ? AGE_BLUE : "transparent",
								color: option.id === props.value ? "#fff" : "inherit"
							},
							children: option.label
						}, option.id))
					})
				]
			});
		}
		/** Readout of what the plugin has actually done, not what it planned. */
		function Readout({ settings, status, locale }) {
			const preset = effectivePreset(settings);
			const usedSeconds = (status.budgetUsedMs / 1e3).toFixed(1);
			const capacitySeconds = (status.budgetCapacityMs / 1e3).toFixed(0);
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				style: {
					display: "grid",
					gridTemplateColumns: "repeat(auto-fit, minmax(150px, 1fr))",
					gap: 10
				},
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: hint,
						children: pick(locale, {
							zh: "本分钟已卡次数",
							en: "Hitches this minute"
						})
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: {
							fontSize: 18,
							fontWeight: 600
						},
						children: status.hitchesThisMinute
					})] }),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: hint,
						children: pick(locale, {
							zh: "本分钟已占用",
							en: "Blocked this minute"
						})
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: {
							fontSize: 18,
							fontWeight: 600
						},
						children: [
							usedSeconds,
							"s",
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
								style: {
									fontSize: 12,
									opacity: .6
								},
								children: [
									" / ",
									capacitySeconds,
									"s"
								]
							})
						]
					})] }),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: hint,
						children: pick(locale, {
							zh: "本次会话实测阻塞",
							en: "Measured this session"
						})
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: {
							fontSize: 18,
							fontWeight: 600
						},
						children: [(status.measuredMs / 1e3).toFixed(1), "s"]
					})] }),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						style: hint,
						children: pick(locale, {
							zh: "单次上限",
							en: "Hard per-hitch cap"
						})
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: {
							fontSize: 18,
							fontWeight: 600
						},
						children: [(preset.hitchMs[1] / 1e3).toFixed(1), "s"]
					})] })
				]
			});
		}
		/**
		* The four lines every Chinese game is legally obliged to show on launch.
		*
		* Reproduced here because it is the last piece of the ritual this plugin was
		* missing: anyone who has played a domestic game has read these exact sentences
		* more times than they have read the game's own name, and their absence would be
		* more conspicuous than their presence. The copy is rewritten to fit, but the
		* rhythm — four lines, four clauses, one comma each — is the joke.
		*/
		const ADVISORY = [
			{
				zh: "抵制不良ai，拒绝盗版token",
				en: "Resist bad AI, refuse pirated tokens"
			},
			{
				zh: "注意自我保护，谨防受骗上当",
				en: "Protect yourself; beware of scams"
			},
			{
				zh: "适度ask益脑，沉迷reply伤身",
				en: "Asking in moderation sharpens the mind; replying to excess harms the body"
			},
			{
				zh: "合理安排时间，享受健康生活",
				en: "Manage your time well; enjoy a healthy life"
			}
		];
		/**
		* The launch advisory, styled the way the real one is: a bordered box, small
		* type, no decoration.
		*
		* @param props - active language.
		* @returns the advisory block.
		*/
		function Advisory({ locale }) {
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				style: {
					marginTop: 18,
					padding: "12px 16px",
					border: "1px solid rgba(128, 128, 128, 0.28)",
					borderRadius: 8,
					display: "flex",
					flexDirection: "column",
					gap: 4
				},
				children: ADVISORY.map((line) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
					style: {
						...hint,
						opacity: .75
					},
					children: pick(locale, line)
				}, line.zh))
			});
		}
		/**
		* The AGE settings page.
		*
		* @param props - see {@link AgeSectionProps}.
		* @returns the page.
		*/
		function AgeSection(props) {
			const { settings, status, locale, onSet } = props;
			const t = (copy) => pick(locale, copy);
			const symptoms = [
				{
					key: "mouse",
					label: {
						zh: "鼠标延滞",
						en: "Pointer stall"
					},
					description: {
						zh: "屏幕上的指针本身停住不动，你手还在移，它却冻在原位，几秒后拖回你手的位置。",
						en: "The pointer itself stops: your hand keeps moving, it stays where it was, and a beat later it drags across to catch up."
					}
				},
				{
					key: "text",
					label: {
						zh: "文字停滞",
						en: "Text stalls"
					},
					description: {
						zh: "AI 回复输出时出现三种形态：中途停住、几秒后把攒下的内容一次性吐出来；一块一块地跳出来而不是顺滑地爬；或者被横向拉一下再弹回。文字不会丢，只是被推迟。",
						en: "Three shapes of the same thing while a reply streams: it stops and later dumps its backlog at once; it arrives in lumps instead of flowing; or it gets pulled sideways and snaps back. No text is lost, only delayed."
					}
				},
				{
					key: "interaction",
					label: {
						zh: "交互停滞",
						en: "Interaction stalls"
					},
					description: {
						zh: "扫描窗口内弹出的设置框、菜单会延迟入场，切换会话也会顿一下。",
						en: "Dialogs and menus that arrive during a scan window fade in late; switching conversations stutters."
					}
				}
			];
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				"data-dsh-age-section": "",
				style: { paddingBottom: 24 },
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: {
							display: "flex",
							alignItems: "center",
							gap: 14,
							marginBottom: 6
						},
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(AgeMark, { width: 112 }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
							style: {
								fontSize: 12,
								opacity: .62
							},
							children: t({
								zh: "AGE安全组件",
								en: "AGE security component"
							})
						})]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("p", {
						style: {
							...hint,
							marginBottom: 18
						},
						children: t({
							zh: "本页由社区插件 dsh-age 提供，非 DeepSeek 官方功能。它不会读写你的会话内容、账号或文件——所有卡顿都在本地浏览器里产生，刷新即消失。",
							en: "This page is provided by the community plugin dsh-age. It is not a DeepSeek feature. It does not read or write your sessions, account or files — every stall is produced locally in your browser and disappears on refresh."
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: card,
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
							style: heading,
							children: t({
								zh: "总开关",
								en: "Master switch"
							})
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Toggle, {
							label: t({
								zh: "启用 AGE 扫描模拟",
								en: "Enable AGE scan simulation"
							}),
							description: t({
								zh: "关闭后立即停止一切卡顿，并记住这个选择。也可以随时按 Ctrl+Alt+Shift+A 暂停 5 分钟。",
								en: "Turning this off stops every stall immediately and remembers the choice. Ctrl+Alt+Shift+A pauses for five minutes at any time."
							}),
							checked: settings.enabled,
							onChange: (next) => next ? props.onEnable() : props.onDisable()
						})]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: card,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								style: heading,
								children: t({
									zh: "扫描强度",
									en: "Scan intensity"
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Choice, {
								label: t({
									zh: "档位",
									en: "Preset"
								}),
								value: settings.presetId,
								options: PRESET_ORDER.map((id) => ({
									id,
									label: pick(locale, PRESET_LABEL[id])
								})),
								onChange: (id) => onSet({ presetId: id })
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								style: {
									...hint,
									marginTop: 4
								},
								children: pick(locale, PRESET_BLURB[settings.presetId])
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("hr", { style: rule }),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Toggle, {
								label: t({
									zh: "三种症状同时触发",
									en: "Symptoms fire together"
								}),
								description: t({
									zh: "开启时，鼠标、文字、交互都由同一次扫描同时触发。关闭后三者各自独立随机。",
									en: "On, one scan produces every symptom in the same instant. Off, the three run on independent random schedules."
								}),
								checked: settings.correlate,
								onChange: (next) => onSet({ correlate: next })
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: card,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								style: heading,
								children: t({
									zh: "症状",
									en: "Symptoms"
								})
							}),
							symptoms.map((symptom) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(Toggle, {
								label: t(symptom.label),
								description: t(symptom.description),
								checked: settings[symptom.key],
								onChange: (next) => onSet({ [symptom.key]: next })
							}, symptom.key)),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("hr", { style: rule }),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Choice, {
								label: t({
									zh: "鼠标延滞的实现方式",
									en: "Pointer mechanism"
								}),
								description: t({
									zh: "「指针冻结」是默认方式，也是唯一能让指针本身上滞的一种：系统光标由操作系统绘制，不在页面里，所以网页冻不住它——只能把真光标藏起来、自己画一个，阻塞期间那个画出来的光标就是静止的，恢复后再拖回你手的位置。「仅冻帧」保持指针原样，只卡页面内容，不绘制也不隐藏任何东西，因此不可能出错。「精确输入延迟」是实验性方案，让界面按几百毫秒前的位置响应——但布局变化时可能把点击落到你没瞄准的控件上，所以无法证明安全的点击会被直接丢弃（宁可让你重点一次）。",
									en: "Cursor freeze (default) is the only mechanism that makes the pointer itself lag: the platform cursor is compositor-drawn and outside the page, so it cannot be frozen — it has to be hidden, with a drawn one taking its place. That drawn cursor sits still through the block, then drags back to your hand. Page freeze alone leaves the pointer untouched: nothing is drawn or hidden, so it cannot misbehave. Precise input delay is experimental — it makes the UI react to where the pointer was, and a layout shift can land a click on a control you never aimed at, so any press that cannot be proven safe is dropped rather than re-aimed."
								}),
								value: settings.mouseMode,
								options: [
									{
										id: "cursor",
										label: t({
											zh: "指针冻结（默认）",
											en: "Cursor freeze (default)"
										})
									},
									{
										id: "hitch",
										label: t({
											zh: "仅冻帧",
											en: "Page freeze only"
										})
									},
									{
										id: "input-lag",
										label: t({
											zh: "精确输入延迟（实验）",
											en: "Precise input delay (experimental)"
										})
									}
								],
								onChange: (mode) => onSet({ mouseMode: mode })
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: card,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								style: heading,
								children: t({
									zh: "状态",
									en: "Status"
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Readout, {
								settings,
								status,
								locale
							}),
							status.lastSuppression !== null && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								style: {
									...hint,
									marginTop: 10
								},
								children: [t({
									zh: "最近一次未执行的扫描原因：",
									en: "Last scan not run because: "
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("code", { children: status.lastSuppression })]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: props.onScanNow,
								style: {
									marginTop: 12,
									padding: "6px 14px",
									borderRadius: 8,
									border: `1px solid ${AGE_BLUE}`,
									background: "transparent",
									color: AGE_BLUE,
									fontSize: 12,
									cursor: "pointer"
								},
								children: t({
									zh: "现在扫描一次",
									en: "Scan now"
								})
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: card,
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								style: heading,
								children: t({
									zh: "界面",
									en: "Interface"
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Toggle, {
								label: t({
									zh: "显示角落安全组件徽标",
									en: "Show the corner badge"
								}),
								checked: settings.hud,
								onChange: (next) => onSet({ hud: next })
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Choice, {
								label: t({
									zh: "徽标位置",
									en: "Badge corner"
								}),
								value: settings.hudCorner,
								options: [
									{
										id: "top-right",
										label: t({
											zh: "右上",
											en: "Top right"
										})
									},
									{
										id: "top-left",
										label: t({
											zh: "左上",
											en: "Top left"
										})
									},
									{
										id: "bottom-right",
										label: t({
											zh: "右下",
											en: "Bottom right"
										})
									},
									{
										id: "bottom-left",
										label: t({
											zh: "左下",
											en: "Bottom left"
										})
									}
								],
								onChange: (corner) => onSet({ hudCorner: corner })
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Choice, {
								label: t({
									zh: "扫描提示频率",
									en: "Announcement frequency"
								}),
								description: t({
									zh: "多大概率弹出「正在扫描」提示条。",
									en: "How often a scan announces itself with a toast."
								}),
								value: settings.toastChance >= 60 ? "loud" : settings.toastChance >= 20 ? "normal" : "quiet",
								options: [
									{
										id: "quiet",
										label: t({
											zh: "安静",
											en: "Quiet"
										})
									},
									{
										id: "normal",
										label: t({
											zh: "正常",
											en: "Normal"
										})
									},
									{
										id: "loud",
										label: t({
											zh: "啰嗦",
											en: "Chatty"
										})
									}
								],
								onChange: (level) => onSet({ toastChance: level === "loud" ? 80 : level === "normal" ? 35 : 8 })
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								style: {
									...hint,
									marginTop: 6
								},
								children: [t({
									zh: "复现种子：",
									en: "Reproduction seed: "
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("code", { children: settings.seed })]
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("hr", { style: rule }),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: {
							display: "flex",
							gap: 10,
							flexWrap: "wrap"
						},
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: props.onPanic,
								style: {
									padding: "8px 16px",
									borderRadius: 8,
									border: "1px solid rgba(128,128,128,0.4)",
									background: "transparent",
									color: "inherit",
									fontSize: 13,
									cursor: "pointer"
								},
								children: t({
									zh: "暂停 5 分钟",
									en: "Pause for 5 minutes"
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: props.onDisable,
								style: {
									padding: "8px 16px",
									borderRadius: 8,
									border: "1px solid rgba(197, 52, 42, 0.55)",
									background: "transparent",
									color: "#c5342a",
									fontSize: 13,
									cursor: "pointer"
								},
								children: t({
									zh: "彻底关闭",
									en: "Turn off"
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: props.onReset,
								style: {
									padding: "8px 16px",
									borderRadius: 8,
									border: "1px solid rgba(128,128,128,0.4)",
									background: "transparent",
									color: "inherit",
									fontSize: 13,
									cursor: "pointer"
								},
								children: t({
									zh: "恢复默认设置",
									en: "Reset to defaults"
								})
							})
						]
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(Advisory, { locale })
				]
			});
		}
		//#endregion
		//#region src/client/hud/AgeShield.tsx
		/**
		* The badge: the mark on its plate, with the breathing light.
		*
		* Split from `AgeMark.tsx` because the two change for different reasons. The
		* mark is brand artwork that should only ever be re-exported from the source
		* image; the badge is chrome that responds to what the scanner is doing —
		* field colour, glow, breath rate, size. Keeping them apart means retuning the
		* badge can never accidentally distort the letterforms, which is the mistake
		* this pair was split up to fix.
		*/
		/** Plate colour per phase. */
		const PLATE_COLOR = {
			protected: AGE_BLUE,
			scanning: AGE_SCAN_AMBER,
			blocking: AGE_HITCH_RED
		};
		/** Milliseconds per breath cycle: faster means busier. */
		const BREATH_MS = {
			protected: 3200,
			scanning: 900,
			blocking: 350
		};
		/**
		* The corner badge.
		*
		* The letterforms stay white in every phase — that is how the source artwork is
		* built, and recolouring them would make the badge stop reading as the brand.
		* Only the field behind them changes, which is also how a real security
		* component signals state.
		*
		* @param props - see {@link AgeBadgeProps}.
		* @returns the badge.
		*/
		function AgeBadge({ phase, title }) {
			const color = PLATE_COLOR[phase];
			return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				"data-dsh-age-breath": "",
				style: {
					display: "inline-flex",
					borderRadius: 10,
					boxShadow: `0 0 10px ${AGE_GLOW}`,
					["--dsh-age-breath-ms"]: `${BREATH_MS[phase]}ms`,
					...phase === "protected" ? {} : { filter: `drop-shadow(0 0 6px ${color})` }
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AgeMark, {
					width: 46,
					plate: color,
					title
				})
			});
		}
		//#endregion
		//#region src/client/hud/AgeHud.tsx
		/**
		* The corner badge and its announcements: the part that makes the stutter read
		* as a *product*.
		*
		* A stall with no chrome is a bug. A stall with a breathing badge, a status line
		* and an occasional announcement is a security component doing its job — which
		* is the entire joke, and also the reason this file exists rather than the
		* effects speaking for themselves.
		*
		* ## Two layout facts that decide the implementation
		*
		* The `shell.overlay` layer is `position: absolute; inset: 0; pointer-events:
		* none` — but with a `> *` rule that hands pointer events *back* to every direct
		* child. So the HUD root, being a direct child, must set `pointer-events: none`
		* explicitly and re-enable it only on the few controls that need it. Getting
		* this wrong does not look broken; it silently eats every click in the corner of
		* the screen.
		*
		* The frame above the layer is `position: relative; overflow: hidden` with no
		* transform, filter or containment ancestor, so `position: fixed` escapes the
		* clip and resolves against the viewport. A `position: absolute` badge would be
		* clipped at the frame's edge.
		*
		* Styles are inline throughout: the host's CSS-module class names are per-build
		* hashes and cannot be imported from outside.
		*/
		/** Where the badge sits, keyed by the setting. */
		const CORNER = {
			"top-right": {
				top: 14,
				right: 14
			},
			"top-left": {
				top: 14,
				left: 14
			},
			"bottom-right": {
				bottom: 14,
				right: 14
			},
			"bottom-left": {
				bottom: 14,
				left: 14
			}
		};
		/** What the badge should show, from what the engine reports. */
		function badgePhase(status) {
			if (status.state === "hitching" || status.frozen) return "blocking";
			if (status.state === "scanning") return "scanning";
			return "protected";
		}
		/** The status line, localized. */
		function statusText(status, locale) {
			if (status.paused) return pick(locale, {
				zh: "已暂停",
				en: "Paused"
			});
			if (status.state === "disabled") return pick(locale, {
				zh: "已关闭",
				en: "Off"
			});
			if (status.state === "suspended") return pick(locale, {
				zh: "已暂停",
				en: "Paused"
			});
			if (status.frozen) return pick(locale, {
				zh: "正在扫描 · 环境阻塞",
				en: "Scanning · blocked"
			});
			if (status.state === "scanning") return status.heavy ? pick(locale, {
				zh: "启动扫描中…",
				en: "Launch scan…"
			}) : pick(locale, {
				zh: "正在扫描…",
				en: "Scanning…"
			});
			return pick(locale, {
				zh: "已保护",
				en: "Protected"
			});
		}
		/**
		* The badge.
		*
		* @param props - see {@link AgeHudProps}.
		* @returns the corner HUD and its announcements.
		*/
		function AgeHud(props) {
			const { status, settings, toasts, locale, onPanic, onDisable, onPreset, onToggleCollapsed } = props;
			const [menuOpen, setMenuOpen] = (0, react.useState)(false);
			const phase = badgePhase(status);
			const expanded = !settings.hudCollapsed || menuOpen;
			return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [status.frozen && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				"aria-hidden": "true",
				"data-dsh-age-scanline": "",
				style: {
					position: "fixed",
					top: 0,
					left: 0,
					right: 0,
					height: 2,
					zIndex: 29,
					pointerEvents: "none",
					background: `linear-gradient(90deg, transparent, rgba(26, 110, 208, 0.55), transparent)`,
					boxShadow: `0 0 18px 4px rgba(26, 110, 208, 0.55)`
				}
			}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				"data-dsh-age-hud": "",
				style: {
					position: "fixed",
					zIndex: 30,
					display: "flex",
					flexDirection: "column",
					alignItems: "flex-end",
					gap: 8,
					pointerEvents: "none",
					fontFamily: "inherit",
					...CORNER[settings.hudCorner]
				},
				children: [
					toasts.map((toast) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						"data-dsh-age-toast": "",
						role: "status",
						style: {
							maxWidth: 320,
							padding: "7px 11px",
							borderRadius: 8,
							background: "rgba(20, 22, 26, 0.92)",
							color: "#fff",
							fontSize: 12,
							lineHeight: 1.45,
							boxShadow: "0 6px 18px rgba(0, 0, 0, 0.28)"
						},
						children: toast.text
					}, toast.id)),
					/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: {
							display: "flex",
							alignItems: "center",
							gap: 9,
							padding: expanded ? "7px 9px" : 5,
							borderRadius: 12,
							background: "rgba(20, 22, 26, 0.92)",
							boxShadow: "0 6px 18px rgba(0, 0, 0, 0.28)",
							pointerEvents: "auto",
							userSelect: "none"
						},
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: () => setMenuOpen((open) => !open),
								"aria-expanded": menuOpen,
								"aria-label": pick(locale, {
									zh: "AGE 安全组件",
									en: "AGE security component"
								}),
								style: {
									display: "flex",
									border: 0,
									padding: 0,
									background: "none",
									cursor: "pointer"
								},
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AgeBadge, {
									phase,
									title: pick(locale, {
										zh: "AGE 安全组件",
										en: "AGE security component"
									})
								})
							}),
							expanded && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								style: {
									display: "flex",
									flexDirection: "column",
									gap: 2,
									paddingRight: 4
								},
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
									style: {
										color: "#fff",
										fontSize: 12,
										fontWeight: 600,
										lineHeight: 1.2
									},
									children: statusText(status, locale)
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
									style: {
										color: "rgba(255,255,255,0.62)",
										fontSize: 10.5,
										lineHeight: 1.2
									},
									children: [
										pick(locale, {
											zh: "本分钟 ",
											en: "This minute "
										}),
										status.hitchesThisMinute,
										" · ",
										Math.round(status.budgetUsedMs / 1e3),
										"s/",
										Math.round(status.budgetCapacityMs / 1e3),
										"s"
									]
								})]
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: onToggleCollapsed,
								"aria-label": pick(locale, {
									zh: "折叠状态条",
									en: "Collapse status"
								}),
								style: {
									border: 0,
									background: "none",
									color: "rgba(255,255,255,0.6)",
									cursor: "pointer",
									fontSize: 12,
									lineHeight: 1,
									padding: "2px 3px"
								},
								children: settings.hudCollapsed ? "‹" : "›"
							})
						]
					}),
					menuOpen && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						style: {
							display: "flex",
							flexDirection: "column",
							gap: 6,
							padding: 10,
							borderRadius: 12,
							background: "rgba(20, 22, 26, 0.95)",
							boxShadow: "0 10px 28px rgba(0, 0, 0, 0.34)",
							pointerEvents: "auto",
							minWidth: 180
						},
						children: [
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								style: {
									color: "rgba(255,255,255,0.6)",
									fontSize: 10.5
								},
								children: pick(locale, {
									zh: "扫描强度",
									en: "Intensity"
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
								style: {
									display: "flex",
									gap: 4
								},
								children: PRESET_ORDER.map((id) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
									type: "button",
									onClick: () => onPreset(id),
									style: {
										flex: 1,
										padding: "4px 6px",
										borderRadius: 6,
										border: `1px solid ${id === settings.presetId ? "#1a6ed0" : "rgba(255,255,255,0.18)"}`,
										background: id === settings.presetId ? "#1a6ed0" : "transparent",
										color: id === settings.presetId ? "#fff" : "rgba(255,255,255,0.8)",
										fontSize: 11,
										cursor: "pointer"
									},
									children: pick(locale, PRESET_LABEL[id])
								}, id))
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: onPanic,
								style: {
									padding: "5px 8px",
									borderRadius: 6,
									border: "1px solid rgba(255,255,255,0.18)",
									background: "transparent",
									color: "rgba(255,255,255,0.85)",
									fontSize: 11,
									cursor: "pointer"
								},
								children: pick(locale, {
									zh: "暂停 5 分钟",
									en: "Pause 5 min"
								})
							}),
							/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
								type: "button",
								onClick: onDisable,
								style: {
									padding: "5px 8px",
									borderRadius: 6,
									border: "1px solid rgba(255,255,255,0.18)",
									background: "transparent",
									color: "rgba(255,255,255,0.85)",
									fontSize: 11,
									cursor: "pointer"
								},
								children: pick(locale, {
									zh: "彻底关闭",
									en: "Turn off"
								})
							})
						]
					})
				]
			})] });
		}
		//#endregion
		//#region src/client/scheduler.ts
		/** A hitch is never allowed to block longer than this, whatever any preset says. */
		const MAX_CONTINUOUS_BLOCK_MS = 3e3;
		/**
		* A scan's duration is drawn once, up front, so the window is a single plan
		* rather than a chain of dependent draws. That makes a scan reproducible from
		* the seed and makes the "hitches never overlap" invariant provable by
		* construction instead of by watching.
		*/
		var AceScheduler = class {
			#preset;
			#rng;
			#host;
			/** Symptoms this instance may produce; a subset of the preset's when uncorrelated. */
			#kinds;
			#state = "idle";
			#scanCounter = 0;
			#hitchCounter = 0;
			#cancels = /* @__PURE__ */ new Set();
			/** Not before this instant, whatever the schedule says. */
			#notBeforeMs = 0;
			/**
			* @param preset - intensity specification.
			* @param rng - this instance's own stream.
			* @param host - clock, admission and reporting.
			* @param kinds - symptoms to produce; defaults to everything the preset lists.
			*/
			constructor(preset, rng, host, kinds) {
				this.#preset = preset;
				this.#rng = rng;
				this.#host = host;
				this.#kinds = kinds ?? preset.kinds;
			}
			/** Current state, for tests and the HUD. */
			get state() {
				return this.#state;
			}
			/**
			* Swap the intensity table.
			*
			* Safe mid-scan: the in-flight scan finishes on its old plan and the next one
			* is drawn from the new preset. Re-planning a scan the user is already inside
			* would mean hitches appearing that were never announced to the HUD.
			*
			* @param preset - the replacement.
			*/
			setPreset(preset) {
				this.#preset = preset;
				if (this.#state === "idle") {
					this.#cancelAll();
					this.#scheduleScan();
				}
			}
			/**
			* Begin scheduling.
			*
			* @param graceMs - quiet time before the first scan; the plugin uses this to
			* let the app finish booting before it starts misbehaving.
			*/
			start(graceMs = 0) {
				if (this.#state === "disposed" || this.#state === "suspended") return;
				this.#notBeforeMs = this.#host.now() + graceMs;
				this.#scheduleScan();
			}
			/**
			* Stop everything and cancel the plan, without tearing down.
			*
			* @param reason - reported for the HUD's status line.
			*/
			suspend(reason) {
				if (this.#state === "disposed") return;
				this.#cancelAll();
				this.#setState("suspended");
				this.#host.emit({
					type: "suppressed",
					reason
				});
			}
			/**
			* Resume after a suspension.
			*
			* @param graceMs - quiet time before scheduling restarts.
			*/
			resume(graceMs) {
				if (this.#state === "disposed") return;
				this.#cancelAll();
				this.#setState("idle");
				this.start(graceMs);
			}
			/**
			* Open a scan right now, ignoring the quiet gap.
			*
			* This is what makes the plugin demonstrable: a reviewer should not have to
			* wait out a 5–15 second gap to find out whether anything works. Admission is
			* still consulted for every hitch inside it, so pressing this with the
			* settings page open correctly produces nothing at all.
			*/
			scanNow() {
				if (this.#state === "disposed" || this.#state === "suspended") return;
				this.#cancelAll();
				this.#setState("idle");
				this.#openScan();
			}
			/** Terminal. Cancels every timer and refuses to run again. */
			dispose() {
				if (this.#state === "disposed") return;
				this.#cancelAll();
				this.#setState("disposed");
			}
			/** Draw one scan's complete plan and open it. */
			#scheduleScan() {
				if (this.#state !== "idle") return;
				const gap = this.#rng.range(this.#preset.scanGapMs);
				const wait = Math.max(gap, this.#notBeforeMs - this.#host.now());
				this.#defer(() => this.#openScan(), wait);
			}
			/** Open a scan: draw the window, the bursts, and their symptom mixes. */
			#openScan() {
				if (this.#state !== "idle") return;
				const preset = this.#preset;
				const scanId = this.#scanCounter += 1;
				const heavy = preset.startupHeavyScan && scanId === 1;
				const windowMs = this.#rng.range(preset.scanWindowMs);
				const planned = this.#rng.int(preset.burstsPerScan[0], preset.burstsPerScan[1]);
				const { bursts, endMs } = this.#planBursts(planned, windowMs, heavy);
				this.#setState("scanning");
				this.#host.emit({
					type: "scanStart",
					scanId,
					plannedHitches: bursts.length,
					heavy
				});
				if (bursts.length === 0) {
					this.#defer(() => this.#closeScan(scanId), windowMs);
					return;
				}
				for (const burst of bursts) this.#deferAt(burst.offsetMs, () => this.#fireBurst(scanId, burst, heavy));
				this.#deferAt(endMs, () => this.#closeScan(scanId));
			}
			/**
			* Lay out a scan's hitches.
			*
			* Offsets are drawn across the first 80% of the window so the last hitch has
			* room to finish inside it, then pushed forward until each sits at least one
			* refractory period after the previous one *ends* — which is the invariant
			* that guarantees two blocks can never overlap, and therefore that the
			* longest continuous block is bounded by a single `hitchMs` draw.
			*/
			#planBursts(count, windowMs, heavy) {
				const preset = this.#preset;
				const bursts = [];
				let cursor = this.#rng.range([0, Math.min(windowMs * .15, 2e3)]);
				let remaining = count;
				while (remaining > 0) {
					const durationMs = Math.min(this.#rng.range(preset.hitchMs), MAX_CONTINUOUS_BLOCK_MS);
					const kinds = this.#kinds.filter(() => this.#rng.next() < .85);
					if (cursor + durationMs > windowMs) break;
					bursts.push({
						offsetMs: cursor,
						durationMs,
						kinds
					});
					cursor += durationMs + preset.refractoryMs;
					remaining -= 1;
					if (heavy && remaining > 0) {
						const extra = Math.min(this.#rng.range(preset.hitchMs), MAX_CONTINUOUS_BLOCK_MS);
						if (cursor + extra > windowMs) break;
						bursts.push({
							offsetMs: cursor,
							durationMs: extra,
							kinds
						});
						cursor += extra + preset.refractoryMs;
						remaining -= 1;
					}
				}
				return {
					bursts,
					endMs: cursor
				};
			}
			/** Attempt one hitch, through the admission choke point. */
			#fireBurst(scanId, burst, heavy) {
				if (this.#state !== "scanning" && this.#state !== "refractory") return;
				if (burst.kinds.length === 0) return;
				const outcome = this.#host.admit({
					durationMs: burst.durationMs,
					kinds: burst.kinds,
					heavy
				});
				const granted = outcome.grantedMs;
				if (granted < 200) {
					this.#host.emit({
						type: "suppressed",
						reason: outcome.reason ?? "budget"
					});
					return;
				}
				const hitchId = this.#hitchCounter += 1;
				this.#setState("hitching");
				this.#defer(() => {
					if (this.#state === "hitching") this.#setState("refractory");
				}, granted);
				this.#host.emit({
					type: "hitch",
					hitchId,
					scanId,
					durationMs: granted,
					kinds: burst.kinds,
					heavy
				});
			}
			/** Close the scan and start the quiet gap. */
			#closeScan(scanId) {
				if (this.#state === "disposed" || this.#state === "suspended") return;
				this.#host.emit({
					type: "scanEnd",
					scanId
				});
				this.#setState("idle");
				this.#scheduleScan();
			}
			#setState(state) {
				if (this.#state === state) return;
				this.#state = state;
				this.#host.emit({
					type: "state",
					state
				});
			}
			#defer(fn, ms) {
				this.#deferAt(ms, fn);
			}
			#deferAt(ms, fn) {
				const cancel = this.#host.schedule(() => {
					this.#cancels.delete(cancel);
					fn();
				}, Math.max(0, ms));
				this.#cancels.add(cancel);
			}
			#cancelAll() {
				for (const cancel of this.#cancels) cancel();
				this.#cancels.clear();
			}
		};
		/**
		* Whether a hitch may proceed, given only the ledger and the invariant that two
		* blocks never overlap.
		*
		* Split out from the engine so the arithmetic can be tested without a DOM: the
		* engine's own `admit` composes this with the escape hatches.
		*
		* @param budget - shared duty-cycle ledger.
		* @param now - clock reading.
		* @param request - the planned hitch.
		* @param busy - whether a block is already in flight.
		* @returns granted milliseconds, 0 to refuse.
		*/
		function admitByBudget(budget, now, request, busy) {
			if (busy) return 0;
			if (budget.remaining(now) < 200) return 0;
			const granted = budget.spend(now, Math.min(request.durationMs, MAX_CONTINUOUS_BLOCK_MS));
			return granted < 200 ? 0 : granted;
		}
		//#endregion
		//#region src/client/budget.ts
		/**
		* A sliding-window allowance of main-thread block time.
		*
		* ## What the guarantee is, exactly
		*
		* The invariant enforced is over the **trailing** window: at any instant, the
		* total blocked time in `[now - windowMs, now]` never exceeds `capacityMs`.
		* That is the guarantee that matters — it is what stops a burst of scans from
		* compounding, and it is what a user feels — but it is deliberately weaker than
		* "no window of length `windowMs` anywhere on the timeline exceeds capacity".
		* The stronger claim is not achievable by any online algorithm that must admit
		* or refuse a request without knowing the future, and pretending otherwise in
		* a docstring would be worse than the honest version.
		*
		* The residual gap is closed from the other side: `hitch.ts` caps every single
		* block at a constant, and the scheduler enforces a refractory gap between
		* them, so the worst case is bounded by construction rather than by the ledger.
		*/
		var RollingBudget = class {
			#capacityMs;
			#windowMs;
			#spends = [];
			/**
			* @param capacityMs - blocked milliseconds allowed per window.
			* @param windowMs - length of the sliding window, in milliseconds.
			*/
			constructor(capacityMs, windowMs = 6e4) {
				this.#capacityMs = capacityMs;
				this.#windowMs = windowMs;
			}
			/** Milliseconds already spent inside the current trailing window. */
			usedMs(now) {
				this.#prune(now);
				let total = 0;
				for (const spend of this.#spends) total += spend.ms;
				return total;
			}
			/**
			* Milliseconds still available at `now`.
			* @param now - scheduler clock reading.
			* @returns the remaining allowance, never negative.
			*/
			remaining(now) {
				return Math.max(0, this.#capacityMs - this.usedMs(now));
			}
			/**
			* Record a block that has already been admitted.
			*
			* Callers must have gone through {@link spend}; this exists for replaying a
			* known-good history in tests.
			*
			* @param now - scheduler clock reading.
			* @param ms - milliseconds blocked.
			*/
			record(now, ms) {
				this.#prune(now);
				this.#spends.push({
					at: now,
					ms
				});
			}
			/**
			* Ask for `requested` milliseconds and take whatever is granted.
			*
			* A partial grant is not a failure: a 400 ms hitch where only 250 ms was
			* affordable is still a hitch, and refusing outright whenever the request
			* does not fit would let a full ledger silently disable the plugin.
			*
			* @param now - scheduler clock reading.
			* @param requested - milliseconds the caller wants.
			* @returns the granted milliseconds, which may be 0.
			*/
			spend(now, requested) {
				const granted = Math.min(requested, this.remaining(now));
				if (granted > 0) this.#spends.push({
					at: now,
					ms: granted
				});
				return granted;
			}
			/** Forget every recorded block, so a fresh budget starts at `now`. */
			reset() {
				this.#spends = [];
			}
			/** Drop spends that have aged out of the trailing window. */
			#prune(now) {
				const cutoff = now - this.#windowMs;
				if (this.#spends.length === 0 || (this.#spends[0]?.at ?? now) > cutoff) return;
				this.#spends = this.#spends.filter((spend) => spend.at > cutoff);
			}
		};
		//#endregion
		//#region src/client/rng.ts
		/**
		* mulberry32 — 32 bits of state, no allocation, and a short enough body to
		* audit. Chosen over a hash-based generator because the stream must survive
		* being seeded from an arbitrary string and then advanced hundreds of times
		* without drifting.
		*
		* @param seed - 32-bit seed; only the low 32 bits are used.
		* @returns the generator.
		*/
		function mulberry32(seed) {
			let state = seed >>> 0;
			const next = () => {
				state = state + 1831565813 | 0;
				let t = Math.imul(state ^ state >>> 15, 1 | state);
				t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
				return ((t ^ t >>> 14) >>> 0) / 4294967296;
			};
			return {
				next,
				range: ([min, max]) => min + next() * (max - min),
				int: (min, max) => min + Math.floor(next() * (max - min + 1)),
				pick: (items) => items.length === 0 ? void 0 : items[Math.floor(next() * items.length)]
			};
		}
		/**
		* FNV-1a over UTF-16 code units.
		*
		* Not a security hash and not trying to be — it turns the user-visible seed
		* string into the 32 bits mulberry32 wants, and it is stable across engines,
		* which a `hashCode`-style loop is not guaranteed to be.
		*
		* @param text - seed material.
		* @returns a 32-bit hash.
		*/
		function seedFrom(text) {
			let hash = 2166136261;
			for (let index = 0; index < text.length; index += 1) {
				hash ^= text.charCodeAt(index);
				hash = Math.imul(hash, 16777619);
			}
			return hash >>> 0;
		}
		/**
		* Derive one named stream from the session seed.
		*
		* Streams are decorrelated by mixing the two hashes rather than by offsetting
		* one, because mulberry32's first outputs from adjacent seeds are close enough
		* to correlate visibly in a schedule measured in seconds.
		*
		* @param seed - session seed string.
		* @param stream - which symptom's schedule this is.
		* @returns an independent generator.
		*/
		function streamFor(seed, stream) {
			return mulberry32((seedFrom(seed) ^ seedFrom(`dsh-age:${stream}`)) >>> 0);
		}
		//#endregion
		//#region src/client/registry.ts
		/**
		* Create an undo log.
		*
		* Restores run newest-first: two mutations can touch the same property (a
		* stall and a stretch on the same row), and unwinding in the order they were
		* applied is what leaves the earliest snapshot — the true original — in place.
		*
		* @returns the registry.
		*/
		function createRegistry() {
			const entries = [];
			const push = (restore) => {
				const entry = {
					restore,
					live: true
				};
				entries.push(entry);
				return { release: () => {
					if (!entry.live) return;
					entry.live = false;
					const index = entries.indexOf(entry);
					if (index >= 0) entries.splice(index, 1);
					entry.restore();
				} };
			};
			return {
				push,
				styles: (element, properties) => {
					const previous = /* @__PURE__ */ new Map();
					for (const [property, value] of Object.entries(properties)) {
						previous.set(property, element.style.getPropertyValue(property));
						element.style.setProperty(property, value);
					}
					return push(() => {
						for (const [property, value] of previous) if (value === "") element.style.removeProperty(property);
						else element.style.setProperty(property, value);
					});
				},
				dataset: (element, entriesToSet) => {
					const previous = /* @__PURE__ */ new Map();
					for (const [key, value] of Object.entries(entriesToSet)) {
						previous.set(key, element.dataset[key]);
						element.dataset[key] = value;
					}
					return push(() => {
						for (const [key, value] of previous) if (value === void 0) delete element.dataset[key];
						else element.dataset[key] = value;
					});
				},
				restoreAll: () => {
					for (let index = entries.length - 1; index >= 0; index -= 1) {
						const entry = entries[index];
						if (entry === void 0 || !entry.live) continue;
						entry.live = false;
						entry.restore();
					}
					entries.length = 0;
				},
				get size() {
					return entries.length;
				}
			};
		}
		//#endregion
		//#region src/client/hitch.ts
		/** Monotonic clock, falling back to wall time where `performance` is absent. */
		function nowMs() {
			if (typeof performance !== "undefined" && typeof performance.now === "function") return performance.now();
			return Date.now();
		}
		/**
		* A real spin, not a sleep.
		*
		* The busy loop is the point: a `setTimeout` would hand the thread back to the
		* browser between callbacks and the page would keep painting. Burning the
		* thread is what freezes the frame.
		*/
		const spinWait = { block(ms) {
			if (!(ms > 0)) return;
			const deadline = nowMs() + ms;
			while (nowMs() < deadline);
		} };
		/** Performance-mark names, so a profiler trace is readable without a key. */
		const HITCH_START = "dsh-age:hitch-start";
		const HITCH_END = "dsh-age:hitch-end";
		const HITCH_MEASURE = "dsh-age:hitch";
		/**
		* Block the thread, and leave a measurement behind.
		*
		* The timing is how the settings page reports *measured* blocked time next to
		* the ledger's planned time. Without it the plugin would be asking users to take
		* its honesty on faith, and the whole safety argument is that they should not
		* have to.
		*
		* Note what the return value is for. It is the number the caller should
		* accumulate, and it is returned rather than read back out of the Performance
		* API on purpose: Chromium caps its user-timing buffer at 250 entries and
		* silently drops `mark`/`measure` calls once it is full, so a total derived from
		* `getEntriesByName` stops growing after a few minutes and quietly under-reports
		* — the one number in this plugin that must not lie about how much it has cost.
		* The marks below are still emitted, because they make a DevTools trace
		* readable; they are just no longer the source of the total.
		*
		* @param waiter - the blocking strategy; injected so tests use a no-op.
		* @param ms - milliseconds to block.
		* @returns the milliseconds actually burned, which exceeds `ms` on a loaded machine.
		*/
		function performHitch(waiter, ms) {
			const traceable = typeof performance !== "undefined" && typeof performance.measure === "function";
			if (traceable) performance.mark(HITCH_START);
			const startedAt = nowMs();
			waiter.block(ms);
			const elapsed = nowMs() - startedAt;
			if (traceable) {
				performance.mark(HITCH_END);
				try {
					performance.measure(HITCH_MEASURE, HITCH_START, HITCH_END);
				} catch {}
			}
			return elapsed;
		}
		//#endregion
		//#region src/client/timers.ts
		/** The real timers. */
		const realTimers = {
			after: (ms, fn) => {
				const handle = setTimeout(fn, ms);
				return () => clearTimeout(handle);
			},
			frame: (fn) => {
				const handle = requestAnimationFrame(() => fn());
				return () => cancelAnimationFrame(handle);
			}
		};
		//#endregion
		//#region src/client/styles.ts
		/**
		* The plugin's stylesheet.
		*
		* Injected as one `<style data-plugin="dsh-age">` element rather than written
		* as inline styles wherever it is needed, for the two things inline styles
		* cannot express: keyframes, and descendant rules (the animation freeze has to
		* reach every element on the page, which is exactly what an inline style on one
		* node cannot do).
		*
		* The tag is owned and removed by `ensureStyles`/`removeStyles` rather than left
		* for the host's HMR sweep to find, so an uninstall leaves nothing behind.
		*
		* Everything here is namespaced by `data-dsh-age-*` attributes or the
		* `dsh-age` keyframe prefix. No host class name appears in this file, and none
		* can: they are per-build hashes.
		*/
		/** Attribute carrying the plugin's stylesheet, matching the host's own convention. */
		const STYLE_OWNER = "dsh-age";
		/**
		* Build the stylesheet.
		*
		* @returns CSS text, with the keyframes the HUD and the modal entrance need.
		*/
		function ageCss() {
			return `
/* ---------------------------------------------------------------- freeze -- */

/*
 * Pausing every animation and transition on the page during a hitch.
 *
 * This exists because CSS animations run on the compositor: without it, a
 * spinner keeps spinning next to a frozen page, which is the loudest possible
 * tell that the freeze is synthetic. A real engine stall takes its own
 * animations down with it.
 *
 * One very broad selector, costing a whole-tree style recalculation twice per
 * hitch. Acceptable at this page size, and gated behind a setting precisely
 * because it is the most expensive rule here.
 */
html[data-dsh-age-hitching][data-dsh-age-freeze-anim] *,
html[data-dsh-age-hitching][data-dsh-age-freeze-anim] *::before,
html[data-dsh-age-hitching][data-dsh-age-freeze-anim] *::after {
  animation-play-state: paused !important;
  transition: none !important;
}

/*
 * The transcript column is a plain scrolling box and Chromium may promote it to
 * its own compositor layer, in which case a wheel scroll keeps gliding while
 * everything else is frozen. 'overscroll-behavior' will not stop that on every
 * engine, but it stops the chained bounce that reads as "still alive", and the
 * manual checklist covers the rest.
 */
html[data-dsh-age-hitching] {
  overscroll-behavior: none;
}

/* ------------------------------------------------------------ text stall -- */

/*
 * The frozen row. 'overflow: hidden' is set inline alongside a pinned
 * max-height; this only carries the visual tell that the app is struggling.
 */
[data-dsh-age-locked] {
  transition: opacity 200ms ease-out;
}

/* The frame-drop gate's affordance: the response dims as it lumps in. */
[data-dsh-age-chunking] {
  opacity: 0.93;
  transition: opacity 120ms linear;
}

/* ----------------------------------------------------------------- modal -- */

/*
 * Delayed entrance for a dialog the scan intercepted.
 *
 * The release lives entirely in CSS, which is the point: 'animation-fill-mode:
 * both' holds the from-state through the delay and then plays the entrance with
 * no script involvement. If the plugin's timer never fires, if it is disposed
 * mid-stall, if a later hook throws — the dialog still becomes fully visible.
 * A JS-driven release would have a failure mode where the user is left staring
 * at an invisible modal; this cannot.
 *
 * The '--dsh-age-stall-ms' delay is the only thing JS sets.
 */
@keyframes dsh-age-entrance {
  from { opacity: 0; transform: scale(0.985); }
  to   { opacity: 1; transform: none; }
}
[data-dsh-age-stalled] {
  animation: dsh-age-entrance 260ms cubic-bezier(0.2, 0.7, 0.3, 1) var(--dsh-age-stall-ms, 400ms) both;
}

/*
 * Deliberately NOT done: 'pointer-events: none' on the stalled node. An
 * invisible-but-clickable dialog for half a second harms nothing; an
 * invisible-and-unclickable one is a trap, and the mask behind it closes the
 * dialog on click — so a user clicking where the button will be would dismiss
 * the very thing they were reaching for.
 */

/* ---------------------------------------------------------------- cursor -- */

/*
 * Hiding the platform cursor while a drawn one is on screen.
 *
 * The only way a page can make the pointer itself stutter: the real cursor is
 * compositor-drawn and immune to a blocked main thread, so it has to be taken
 * out of the picture for the block's duration. '!important' on the descendant
 * selector because the host styles 'cursor: pointer' on its own controls, and an
 * inherited 'none' would lose to those.
 *
 * Applied only while 'cursor-lag.ts' has a replacement actually on screen — the
 * attribute is never set without one, because a hidden cursor with nothing drawn
 * in its place is the one outcome worse than a missing effect.
 */
html[data-dsh-age-cursor-hidden],
html[data-dsh-age-cursor-hidden] * {
  cursor: none !important;
}

/* ------------------------------------------------------------------- hud -- */

@keyframes dsh-age-breath {
  0%, 100% { opacity: 0.55; transform: scale(1); }
  50%      { opacity: 1;    transform: scale(1.08); }
}
@keyframes dsh-age-scanline {
  from { transform: translateY(-10vh); opacity: 0; }
  15%  { opacity: 0.9; }
  85%  { opacity: 0.9; }
  to   { transform: translateY(110vh); opacity: 0; }
}
@keyframes dsh-age-spin {
  to { transform: rotate(360deg); }
}
@keyframes dsh-age-toast-in {
  from { opacity: 0; transform: translateY(-6px); }
  to   { opacity: 1; transform: none; }
}

[data-dsh-age-breath] {
  animation: dsh-age-breath var(--dsh-age-breath-ms, 3200ms) ease-in-out infinite;
}
[data-dsh-age-scanline] {
  animation: dsh-age-scanline 1400ms linear forwards;
}
[data-dsh-age-toast] {
  animation: dsh-age-toast-in 180ms ease-out both;
}
[data-dsh-age-spinner] {
  animation: dsh-age-spin 900ms linear infinite;
}

@media (prefers-reduced-motion: reduce) {
  [data-dsh-age-breath], [data-dsh-age-scanline], [data-dsh-age-spinner] {
    animation-duration: 6s;
  }
  /* The entrance still has to happen — it is the effect, not decoration. */
  [data-dsh-age-stalled] {
    animation-duration: 1ms;
  }
}
`.trim();
		}
		/**
		* Install the stylesheet, replacing any previous copy.
		*
		* Replacing rather than skipping matters under HMR: a watched rebuild
		* re-executes the bundle and must be able to ship changed CSS without the old
		* tag shadowing it.
		*
		* @returns a disposer removing the tag.
		*/
		function ensureStyles() {
			const existing = [...document.querySelectorAll(`style[data-plugin="${STYLE_OWNER}"]`)];
			for (const tag of existing) tag.remove();
			const style = document.createElement("style");
			style.dataset["plugin"] = STYLE_OWNER;
			style.textContent = ageCss();
			document.head.append(style);
			return () => style.remove();
		}
		//#endregion
		//#region src/client/dom.ts
		/**
		* The host hooks this plugin reads, and nothing else.
		*
		* Every selector here is a *contract the host publishes on purpose*, not a
		* class name that happens to be there today. That distinction is the whole
		* reason this file exists as a single list: CSS-module class names in the dsh
		* client are per-build hashes and cannot be imported or matched from outside,
		* so a plugin that reached for one would break on the next release with no
		* warning. The `data-*` attributes below are emitted deliberately by
		* `ui-conversation` and `ui-layout`, and the ARIA roles are the platform's own.
		*
		* When a host upgrade moves something, this file is the only place that needs
		* to change — and `tests/dom.spec.ts` will say which hook went missing.
		*/
		/** A transcript row: one step of an assistant turn, one tool call, one message. */
		const FLOW_ROW_SELECTOR = "[data-chat-flow-key]";
		/** The transcript row kind that carries a streaming assistant response. */
		const ASSISTANT_ROW_SELECTOR = "[data-chat-flow-kind=\"assistant-step\"]";
		/**
		* A dialog the user is waiting on.
		*
		* `aria-modal="true"` narrows this to the real ones: the host renders menus and
		* hover cards as `role="dialog"` too, and stalling those as hard as a modal
		* would make the UI feel broken rather than scanned.
		*/
		const MODAL_SELECTOR = "[role=\"dialog\"][aria-modal=\"true\"]";
		/** Marks the plugin's own settings page, which is the hitch-free zone. */
		const SECTION_MARKER = "data-dsh-age-section";
		/**
		* The newest assistant row that is still receiving tokens.
		*
		* Scanned backwards because the streaming row is almost always the last one;
		* forward scanning would walk the whole transcript every time a scan opens.
		*
		* Returns `undefined` rather than the newest assistant row when none is
		* streaming. A row whose `data-streaming` has already cleared is a *finished*
		* message, and freezing it would be a visible lie rather than a timing one.
		*
		* @param root - subtree to search; defaults to the document.
		* @returns the streaming row, if there is one.
		*/
		function streamingRow(root = document) {
			const rows = [...root.querySelectorAll(ASSISTANT_ROW_SELECTOR)];
			for (let index = rows.length - 1; index >= 0; index -= 1) {
				const row = rows[index];
				if (row !== void 0 && row.isConnected && row.querySelector("[data-streaming=\"true\"]") !== null) return row;
			}
		}
		/**
		* The container inside a streaming row whose height the stall freezes.
		*
		* The row itself is the freeze target — it is the element the host pins its
		* own layout around — but the streaming markdown root is the element the
		* stretch targets, because transforming the whole row would move the turn's
		* action strip with it.
		*
		* @param row - a row from {@link streamingRow}.
		* @returns the markdown root, or the row itself if the hook is absent.
		*/
		function streamingBody(row) {
			return row.querySelector("[data-streaming]") ?? row;
		}
		/**
		* The current set of transcript row keys.
		*
		* Used to notice a conversation switch without any hook for "the user clicked a
		* different session" — see `interaction.ts`. The sidebar exposes no `data-*`
		* attributes at all, so identity of the rendered transcript is the only
		* internals-free signal available.
		*
		* @param root - subtree to search; defaults to the document.
		* @returns the row keys, in document order.
		*/
		function flowKeys(root = document) {
			return [...root.querySelectorAll(FLOW_ROW_SELECTOR)].map((row) => row.dataset["chatFlowKey"]).filter((key) => typeof key === "string");
		}
		/**
		* Whether the plugin's own settings page is on screen.
		*
		* The single query that enforces the most important escape hatch: with the
		* control panel open, nothing may block, or the user cannot reach the switch
		* that turns the plugin off. One `querySelector` per hitch attempt — a handful
		* of times a minute — so no observer is warranted.
		*
		* @returns true while the AGE settings page is mounted.
		*/
		function settingsPageOpen() {
			return document.querySelector(`[${SECTION_MARKER}]`) !== null;
		}
		//#endregion
		//#region src/client/effects/stream-stall.ts
		/**
		* Symptom 1 of the text trio: **停滞后恢复** — the response stops, then dumps
		* everything it produced while you were waiting.
		*
		* Mechanism: pin the streaming row's height and clip it, let the model keep
		* writing below the clip line, then reveal the accumulated tail in one go. Text
		* is never lost, only time-shifted, which is exactly what separates a stall from
		* a bug — a user watching it cannot tell that the words already existed.
		*
		* This is the technique `dsh-ads` validated in its reward gate, with one
		* deliberate divergence: it also hides every later transcript row while frozen,
		* and this does not. That plugin needed the space for a card; hiding tool rows
		* here would stack a *structural* lie on top of a *timing* one, and the row
		* count jumping around afterwards would read as the app breaking rather than
		* struggling.
		*
		* ## Three release paths, because a stuck freeze is the one unforgivable bug
		*
		* 1. The scheduled release.
		* 2. An early release when the row detaches or finishes streaming — there is
		*    nothing left to stall, and a frozen finished message is a lie.
		* 3. A self-healing timer that nothing is allowed to cancel, armed at the same
		*    instant as the freeze. Even if path 1's body throws and path 2's observer
		*    never fires, the row un-freezes. Its handle is deliberately kept out of the
		*    disposer's reach: teardown must not be able to disarm it.
		*/
		/** How long after the scheduled release the backstop fires. */
		const BACKSTOP_SLACK_MS$2 = 250;
		/** A row shorter than this has nothing worth freezing. */
		const MIN_FREEZABLE_HEIGHT_PX = 24;
		/**
		* Freeze the streaming row for a while.
		*
		* @param registry - undo log; the freeze registers its restoration here so an
		* unload mid-stall still restores the row.
		* @param timers - timer seam.
		* @param durationMs - how long to hold the freeze.
		* @returns the live stall, or `undefined` when nothing is streaming to stall.
		*/
		function stallStreamRow(registry, timers, durationMs) {
			const row = streamingRow();
			if (row === void 0) return void 0;
			if (!row.isConnected) return void 0;
			const parent = row.parentElement;
			const height = Math.max(1, row.getBoundingClientRect().height, row.scrollHeight);
			if (height < MIN_FREEZABLE_HEIGHT_PX) return void 0;
			const handle = registry.styles(row, {
				"max-height": `${height}px`,
				overflow: "hidden"
			});
			const locked = registry.dataset(row, { dshAgeLocked: "true" });
			let active = true;
			const cancelTimer = timers.after(durationMs, release);
			timers.after(durationMs + BACKSTOP_SLACK_MS$2, release);
			const onStreamEnded = new MutationObserver(() => {
				if (row.querySelector("[data-streaming=\"true\"]") === null) release();
			});
			const onDetached = new MutationObserver(() => {
				if (!row.isConnected) release();
			});
			onStreamEnded.observe(row, {
				attributes: true,
				attributeFilter: ["data-streaming"],
				subtree: true
			});
			if (parent !== null) onDetached.observe(parent, { childList: true });
			const stopObserving = () => {
				onStreamEnded.disconnect();
				onDetached.disconnect();
			};
			function release() {
				if (!active) return;
				active = false;
				cancelTimer();
				stopObserving();
				locked.release();
				handle.release();
			}
			return {
				release,
				get active() {
					return active;
				}
			};
		}
		//#endregion
		//#region src/client/effects/stream-chunk.ts
		/**
		* Symptom 2 of the text trio: **卡断** — the response arrives in lumps instead
		* of flowing.
		*
		* Mechanism: a frame-drop gate. Blocking the main thread inside each animation
		* frame makes React's streaming rerenders coalesce, so instead of a smooth
		* character-by-character crawl the text appears in visible jumps. The browser
		* still paints between frames — it has to, or nothing would ever appear — which
		* is the one place where blocking per frame is correct and blocking once is not.
		*
		* This shares the plugin's single blocking primitive with every other symptom,
		* which is the point: a stall that only the text feels would be a different
		* phenomenon from a stall the whole page feels, and the whole design argument is
		* that there is only one phenomenon.
		*
		* Two guards keep it honest:
		* - It refuses to start while a hitch is in flight, because a frame-gate block
		*   nested inside a spin block would stack the two durations and blow past the
		*   continuous-block cap that `scheduler.ts` promises.
		* - It stops itself the moment nothing is streaming. A frame gate with no text
		*   to lump is just a CPU fire.
		*/
		/** Safety margin on the self-healing stop, in milliseconds. */
		const BACKSTOP_SLACK_MS$1 = 500;
		/**
		* Drop frames for a bounded number of cycles.
		*
		* @param registry - undo log, for the dimming affordance.
		* @param timers - timer seam.
		* @param waiter - the blocking primitive.
		* @param frames - how many frames to block. The caller draws this per hitch.
		* @param blockMs - milliseconds to burn inside each dropped frame.
		* @returns the live gate, or `undefined` when nothing is streaming.
		*/
		function runChunkGate(registry, timers, waiter, frames, blockMs) {
			const row = streamingRow();
			if (row === void 0 || frames <= 0 || blockMs <= 0) return void 0;
			const dim = registry.dataset(row, { dshAgeChunking: "true" });
			let remaining = frames;
			let active = true;
			let cancelFrame;
			const tick = () => {
				if (!active) return;
				if (streamingRow() === void 0) {
					stop();
					return;
				}
				waiter.block(blockMs);
				remaining -= 1;
				if (remaining <= 0) {
					stop();
					return;
				}
				cancelFrame = timers.frame(tick);
			};
			cancelFrame = timers.frame(tick);
			timers.after(frames * 120 + BACKSTOP_SLACK_MS$1, stop);
			function stop() {
				if (!active) return;
				active = false;
				cancelFrame?.();
				cancelFrame = void 0;
				dim.release();
			}
			return {
				stop,
				get active() {
					return active;
				}
			};
		}
		//#endregion
		//#region src/client/effects/stream-stretch.ts
		/**
		* Symptom 3 of the text trio: **拉长** — the text visibly pulls sideways for a
		* beat, then snaps back.
		*
		* Mechanism: a brief geometric distortion of the streaming markdown root. Two
		* expressions of it, chosen by preset:
		*
		* - `letter-spacing` reflows the text. Only the streaming row relayouts, and
		*   nothing downstream can be mispositioned by it.
		* - `transform` reads more like a physical pull and animates on the compositor
		*   for free — but an inline `transform` on an ancestor of a `position: fixed`
		*   descendant creates a containing block, which would misplace any anchored
		*   popover opened from inside the message.
		*
		* That last hazard is why the function refuses outright while a menu or dialog
		* is open inside the row. A briefly misplaced tooltip would be a small bug, but
		* it is a *new* bug the plugin invented, and the low presets never needed the
		* transform in the first place.
		*/
		/** How long the distortion is held, in milliseconds. */
		const STRETCH_HOLD_MS = 160;
		/**
		* Distort the streaming body for a moment.
		*
		* @param registry - undo log; every property set here is individually reversible.
		* @param timers - timer seam.
		* @param scale - horizontal scale; 1.03 is the heavy preset's value.
		* @param mode - how to express it.
		* @returns true when the stretch was applied.
		*/
		function stretchStreamRow(registry, timers, scale, mode) {
			const row = streamingRow();
			if (row === void 0) return false;
			if (row.querySelector(`[role="menu"], [role="dialog"][aria-modal="true"]`) !== null) return false;
			const body = streamingBody(row);
			const undo = mode === "transform" ? registry.styles(body, {
				transform: `scaleX(${scale}) scaleY(${1 + (scale - 1) * .15})`,
				"transform-origin": "left center",
				transition: `transform ${STRETCH_HOLD_MS}ms ease-out`,
				"will-change": "transform"
			}) : registry.styles(body, {
				"letter-spacing": `${((scale - 1) * 12).toFixed(2)}px`,
				transition: `letter-spacing ${STRETCH_HOLD_MS}ms ease-out`
			});
			timers.after(360, () => undo.release());
			timers.after(STRETCH_HOLD_MS, () => undo.release());
			return true;
		}
		//#endregion
		//#region src/client/effects/dom-stall.ts
		/**
		* Symptom 3 of the brief: **everything interactive stalls** — the settings
		* dialog, a conversation switch, a new conversation.
		*
		* ## Observe outcomes, never predict triggers
		*
		* The tempting implementation is a capture-phase click listener matching the
		* settings button, the sidebar rows, the new-conversation button. It is also the
		* fragile one: the sidebar exposes **no** `data-*` hooks at all (verified against
		* the shipped client — there is not one `data-testid` in the whole package), so
		* every one of those matches would be against visible copy or a hashed class
		* name, and would break silently the first time the host rewords a button or a
		* user switches language.
		*
		* So this watches what *arrived* instead. A dialog appearing is unambiguous, and
		* it is the thing the user perceives. The cost is that the stall begins when the
		* dialog mounts rather than before it — which, given the effect is a delayed
		* entrance, is indistinguishable from the real thing.
		*
		* Trigger prediction is still available behind a setting, for anyone who wants
		* the stall to land on the click itself; it is off by default because a
		* copy-matching rule is a maintenance liability dressed as a feature.
		*/
		/** Debounce for the transcript-identity comparison. */
		const SWITCH_DEBOUNCE_MS = 120;
		/**
		* Below this share of rows surviving a swap, the transcript is treated as a
		* different conversation rather than the same one growing.
		*/
		const SWITCH_OVERLAP_THRESHOLD = .5;
		/** Slack after the entrance animation before the attribute is cleaned up. */
		const ENTRANCE_CLEANUP_SLACK_MS = 900;
		/**
		* Delay a dialog's entrance.
		*
		* The release is deliberately **CSS-only**: the attribute selects a keyframe
		* animation with `animation-fill-mode: both`, which holds the dialog invisible
		* through the delay and then plays the entrance with no script involvement. If
		* the plugin's cleanup timer never fires, the dialog still appears.
		*
		* That is strictly safer than the obvious JS-driven version, which has a failure
		* mode where a disposed plugin leaves the user staring at an invisible modal.
		* This function only sets a CSS variable and an attribute; the animation is the
		* guarantee.
		*
		* `pointer-events` is deliberately left alone. An invisible-but-clickable dialog
		* for half a second harms nothing; an invisible-and-unclickable one is a trap,
		* and the mask behind it closes the dialog — so a user clicking where the button
		* is about to be would dismiss the very thing they were reaching for.
		*
		* @param registry - undo log.
		* @param timers - timer seam.
		* @param dialog - the dialog element.
		* @param delayMs - entrance delay.
		*/
		function stallEntrance(registry, timers, dialog, delayMs) {
			if (delayMs <= 0) return;
			const mask = dialog.parentElement?.querySelector("[aria-hidden=\"true\"]");
			const targets = mask === void 0 || mask === null ? [dialog] : [dialog, mask];
			for (const target of targets) {
				const marks = registry.dataset(target, { dshAgeStalled: "true" });
				const variable = registry.styles(target, { "--dsh-age-stall-ms": `${delayMs}ms` });
				timers.after(delayMs + ENTRANCE_CLEANUP_SLACK_MS, () => {
					marks.release();
					variable.release();
				});
			}
		}
		/**
		* Watch the document for arrivals worth stalling.
		*
		* A `MutationObserver` callback runs as a microtask *before* the next paint, so
		* the attribute set here is applied before the node is ever painted in its final
		* state. There is no flash of the un-stalled UI — which is the difference
		* between reading as "the app was slow" and reading as "the app animated in".
		*
		* @param timers - timer seam.
		* @param handlers - what to do with what arrives.
		* @returns a disposer.
		*/
		function watchArrivals(timers, handlers) {
			const seen = /* @__PURE__ */ new WeakSet();
			let previousKeys = flowKeys();
			let cancelSwitch;
			const compareTranscript = () => {
				cancelSwitch?.();
				cancelSwitch = timers.after(SWITCH_DEBOUNCE_MS, () => {
					cancelSwitch = void 0;
					const current = flowKeys();
					const previous = new Set(previousKeys);
					const survived = current.filter((key) => previous.has(key)).length;
					const basis = Math.max(previousKeys.length, current.length, 1);
					const switched = previousKeys.length > 0 && survived / basis < SWITCH_OVERLAP_THRESHOLD;
					previousKeys = current;
					if (switched) handlers.onConversationSwitch();
				});
			};
			const observer = new MutationObserver((records) => {
				let transcriptTouched = false;
				for (const record of records) {
					for (const node of record.addedNodes) {
						if (!(node instanceof HTMLElement)) continue;
						if (node === document.body) continue;
						if (!seen.has(node)) {
							const dialog = node.matches("[role=\"dialog\"][aria-modal=\"true\"]") ? node : node.querySelector(MODAL_SELECTOR);
							if (dialog instanceof HTMLElement) {
								seen.add(node);
								handlers.onArrival(dialog, "modal");
							} else if (node.matches("[role=\"menu\"]")) {
								seen.add(node);
								handlers.onArrival(node, "menu");
							}
						}
						if (node.matches("[data-chat-flow]") || node.querySelector("[data-chat-flow]") !== null) transcriptTouched = true;
					}
					if (record.removedNodes.length > 0 && record.target instanceof HTMLElement) {
						if (record.target.closest("[data-chat-flow]") !== null) transcriptTouched = true;
					}
				}
				if (transcriptTouched) compareTranscript();
			});
			observer.observe(document.body, {
				childList: true,
				subtree: true
			});
			return () => {
				cancelSwitch?.();
				observer.disconnect();
			};
		}
		/** Pointer button events swallowed while armed. */
		const BUTTON_EVENTS = [
			"pointerdown",
			"pointerup",
			"click",
			"mousedown",
			"mouseup"
		];
		/**
		* Create the controller.
		*
		* Listeners are attached immediately and stay attached for the plugin's whole
		* life; they only *act* while armed. Attaching lazily at arm time would miss the
		* press that lands in the same tick as the scan starting — which, given the whole
		* mode exists to punish exactly that press, would be self-defeating.
		*
		* @param timers - timer seam.
		* @param now - clock, injected so tests need not wait in real time.
		* @returns the controller.
		*/
		function createInputLag(timers, now = () => Date.now()) {
			/**
			* Events this controller produced. Per-instance rather than module-level: a
			* shared set would let one instance's replay slip through another's capture
			* listener, and there is no reason to couple them.
			*/
			const replaying = /* @__PURE__ */ new WeakSet();
			let armed = false;
			let presses = [];
			let cancelFlush;
			/** Swallow an event without letting the host observe it. */
			const swallow = (event) => {
				event.stopPropagation();
				event.stopImmediatePropagation();
				event.preventDefault();
			};
			const onPointerDown = (event) => {
				if (!armed || replaying.has(event)) return;
				const { target } = event;
				if (!(target instanceof HTMLElement)) return;
				presses.push({
					clientX: event.clientX,
					clientY: event.clientY,
					target,
					button: event.button,
					buttons: event.buttons,
					pointerId: event.pointerId,
					pointerType: event.pointerType,
					ctrlKey: event.ctrlKey,
					shiftKey: event.shiftKey,
					altKey: event.altKey,
					metaKey: event.metaKey,
					at: now(),
					moved: 0
				});
				swallow(event);
			};
			const onPointerMove = (event) => {
				if (!armed || replaying.has(event)) return;
				const press = presses.findLast((candidate) => candidate.pointerId === event.pointerId);
				if (press === void 0) return;
				press.moved = Math.max(press.moved, Math.hypot(event.clientX - press.clientX, event.clientY - press.clientY));
			};
			/** Whether a held press may be replayed onto the element it originally hit. */
			const isSafeToReplay = (press) => {
				if (now() - press.at > 1500) return false;
				if (!press.target.isConnected) return false;
				if (press.moved > 6) return false;
				const under = document.elementFromPoint(press.clientX, press.clientY);
				if (under === null) return false;
				return under === press.target || press.target.contains(under) || under.contains(press.target);
			};
			/** Re-dispatch one held press at its original coordinates. */
			const replay = (press) => {
				const base = {
					bubbles: true,
					composed: true,
					cancelable: true,
					clientX: press.clientX,
					clientY: press.clientY,
					button: press.button,
					ctrlKey: press.ctrlKey,
					shiftKey: press.shiftKey,
					altKey: press.altKey,
					metaKey: press.metaKey
				};
				const events = [
					new PointerEvent("pointerdown", {
						...base,
						buttons: press.buttons,
						pointerId: press.pointerId,
						pointerType: press.pointerType,
						isPrimary: true
					}),
					new PointerEvent("pointerup", {
						...base,
						buttons: 0,
						pointerId: press.pointerId,
						pointerType: press.pointerType,
						isPrimary: true
					}),
					new MouseEvent("click", {
						...base,
						detail: 1
					})
				];
				for (const event of events) {
					replaying.add(event);
					press.target.dispatchEvent(event);
				}
			};
			const flush = () => {
				cancelFlush = void 0;
				const held = presses;
				presses = [];
				for (const press of held) if (isSafeToReplay(press)) replay(press);
			};
			const onButton = (event) => {
				if (!armed || replaying.has(event)) return;
				swallow(event);
			};
			window.addEventListener("pointerdown", onPointerDown, { capture: true });
			window.addEventListener("pointermove", onPointerMove, {
				capture: true,
				passive: true
			});
			for (const type of BUTTON_EVENTS) window.addEventListener(type, onButton, { capture: true });
			return {
				arm: (ms) => {
					if (armed) return;
					armed = true;
					cancelFlush = timers.after(ms, flush);
				},
				disarm: () => {
					if (!armed) return;
					armed = false;
					cancelFlush?.();
					flush();
				},
				dispose: () => {
					armed = false;
					cancelFlush?.();
					presses = [];
					for (const type of BUTTON_EVENTS) window.removeEventListener(type, onButton, { capture: true });
					window.removeEventListener("pointerdown", onPointerDown, { capture: true });
					window.removeEventListener("pointermove", onPointerMove, { capture: true });
				},
				get heldCount() {
					return presses.length;
				}
			};
		}
		//#endregion
		//#region src/client/effects/cursor-lag.ts
		/**
		* The pointer symptom, for real: a cursor that stops when the thread stops.
		*
		* ## Why this has to exist
		*
		* Every other symptom can be simulated from inside the page. This one cannot.
		* The operating system draws the pointer on the compositor, outside the
		* document, so a busy-wait freezes the page while the cursor keeps gliding over
		* it — which is why the pointer was the one thing the plugin could not
		* reproduce, and why "the mouse lags" was, until this module, a label on a
		* page-wide block rather than an effect of its own.
		*
		* The only way a page can make the pointer itself stutter is to stop showing the
		* pointer: hide the real cursor and draw our own at the last position we saw.
		* During a block nothing runs, so the drawn cursor is frozen where it was while
		* the hidden real one keeps moving under the user's hand. When the thread comes
		* back, the drawn cursor is simply *there*, under the hand, and then the real
		* one takes over.
		*
		* That is the ACE tell, reproduced rather than described: *the mouse moves, the
		* pointer does not, and then it is somewhere else.* The jump must be instant —
		* a cursor that slides into place is an animation, and an animation is the one
		* thing a stalled pointer never looks like.
		*
		* ## The one failure that would be unacceptable
		*
		* This is the only place in the plugin that can leave the user without a visible
		* pointer, and no joke is worth that. So:
		*
		* - The real cursor is hidden only while a drawn one is on screen. If there is
		*   no known position to draw at, nothing is hidden.
		* - The hide lives on `documentElement`, so `RestoreRegistry`'s teardown clears
		*   it along with everything else.
		* - A backstop timer restores the cursor independently of the normal path, and
		*   is armed before the block starts rather than after it.
		* - The engine wraps the whole thing in `try`/`finally` regardless.
		*
		* Four independent ways back, none of which depends on the others working.
		*/
		/** Attribute marking that the real cursor is hidden. */
		const CURSOR_HIDDEN_ATTRIBUTE = "data-dsh-age-cursor-hidden";
		/** Attribute on the drawn cursor element. */
		const CURSOR_MARKER = "data-dsh-age-cursor";
		/** How long the jumped cursor is held before the platform one takes over. */
		const HANDOVER_MS = 60;
		/** Extra time the backstop waits beyond the expected hold. */
		const BACKSTOP_SLACK_MS = 600;
		/**
		* Hard ceiling on a freeze that never gets settled.
		*
		* `settle` is always called in the same task as `freeze`, so this only fires if
		* something threw in between. Comfortably longer than the longest block any
		* preset can ask for, because firing early would snatch the pointer back
		* mid-hitch — but bounded, because a freeze with no settle and no ceiling is a
		* user with no visible pointer.
		*/
		const FREEZE_BACKSTOP_MS = 15e3;
		/**
		* The classic arrow, drawn from the origin at the hotspot, at the platform
		* cursor's own size: about 11.5 by 19 CSS pixels at 100% scaling.
		*
		* Measured against a real Windows pointer rather than eyeballed — an earlier
		* version of this path spanned 13.2 pixels and drew a stroke on top of that,
		* which read as a cursor one size too large next to the real one it replaces.
		* White fill with a dark outline so it stays legible over both light and dark
		* host themes, which is the same trick the platform cursors use.
		*/
		const ARROW_PATH = "M0 0 L0 16.5 L4.2 12.8 L7 19 L9.5 17.9 L6.7 11.9 L11.5 11.5 Z";
		/**
		* Create the controller.
		*
		* The move listener is attached immediately and stays attached for the plugin's
		* whole life: `freeze()` needs a position to draw at, and a listener attached at
		* freeze time would have nothing to draw.
		*
		* @param timers - timer seam.
		* @param registry - undo log shared with the rest of the plugin.
		* @returns the controller.
		*/
		function createCursorLag(timers, registry = createRegistry()) {
			let fix;
			let element;
			let undo;
			let cancelBackstop;
			let frozenAt = 0;
			/**
			* Terminal. Checked by `freeze` because disposal only drops the listener — the
			* last known position stays in memory, so without this a disposed controller
			* would still happily hide the cursor and draw over it, with nothing left
			* listening and no owner expecting it to.
			*/
			let disposed = false;
			const onMove = (event) => {
				fix = {
					x: event.clientX,
					y: event.clientY
				};
			};
			window.addEventListener("pointermove", onMove, {
				capture: true,
				passive: true
			});
			/** Bring the real cursor back and remove the drawing. Idempotent. */
			const restore = () => {
				cancelBackstop?.();
				cancelBackstop = void 0;
				element?.remove();
				element = void 0;
				undo?.release();
				undo = void 0;
			};
			return {
				freeze: () => {
					if (disposed) return;
					if (fix === void 0 || element !== void 0) return;
					if (document.hidden || !document.body) return;
					const cursor = document.createElement("div");
					cursor.setAttribute(CURSOR_MARKER, "");
					cursor.setAttribute("aria-hidden", "true");
					Object.assign(cursor.style, {
						position: "fixed",
						left: "0",
						top: "0",
						width: "0",
						height: "0",
						zIndex: "2147483647",
						pointerEvents: "none",
						willChange: "transform"
					});
					cursor.innerHTML = `<svg width="24" height="24" viewBox="0 0 24 24" aria-hidden="true"><path d="${ARROW_PATH}" fill="#ffffff" stroke="#1b1c1f" stroke-width="1" stroke-linejoin="round"/></svg>`;
					cursor.style.transform = `translate3d(${fix.x}px, ${fix.y}px, 0)`;
					document.body.append(cursor);
					element = cursor;
					frozenAt = performance.now();
					undo = { release: () => document.documentElement.removeAttribute(CURSOR_HIDDEN_ATTRIBUTE) };
					document.documentElement.setAttribute(CURSOR_HIDDEN_ATTRIBUTE, "true");
					registry.push(() => restore());
					cancelBackstop = timers.after(FREEZE_BACKSTOP_MS, restore);
				},
				settle: (holdMs) => {
					const cursor = element;
					const target = fix;
					if (cursor === void 0) return;
					const remaining = Math.max(0, holdMs - (performance.now() - frozenAt));
					cancelBackstop?.();
					cancelBackstop = timers.after(remaining + HANDOVER_MS + BACKSTOP_SLACK_MS, restore);
					timers.after(remaining, () => {
						timers.frame(() => {
							if (element !== cursor) return;
							const destination = fix ?? target;
							if (destination !== void 0) cursor.style.transform = `translate3d(${destination.x}px, ${destination.y}px, 0)`;
							cancelBackstop?.();
							cancelBackstop = timers.after(660, restore);
						});
					});
				},
				restore,
				dispose: () => {
					disposed = true;
					window.removeEventListener("pointermove", onMove, { capture: true });
					fix = void 0;
					restore();
				},
				get active() {
					return element !== void 0;
				}
			};
		}
		//#endregion
		//#region src/client/engine.ts
		/**
		* The engine: the only thing in this plugin that touches the DOM, and the only
		* thing that decides whether a hitch may happen.
		*
		* ## Why the decision is centralised
		*
		* There are six independent reasons a hitch must not fire — the master switch,
		* the settings page being open, a background tab, a grace period after the user
		* clawed control back, a block already in flight, and the duty-cycle ledger. Each
		* symptom could check the ones it cares about, and the result would be six
		* slightly different safety stories that drift apart the first time one of them
		* is edited. Instead every symptom, every symptom's frame gate, every entrance
		* stall and every deferred input window routes through {@link AceEngine.admit}.
		* There is one place to audit and one place to be wrong.
		*
		* ## What a hitch actually does
		*
		* A hitch blocks the main thread. Everything the user perceives — the pointer
		* not responding, the text stopping mid-sentence, the button that needs a second
		* click — is downstream of that one block, which is precisely how the real thing
		* works. The named symptoms then decide *how* they express themselves within it:
		* the stall pins the transcript row, the gate drops frames, the stretch skews the
		* text, and interaction stalls ride the scan window rather than the block.
		*/
		/** Milliseconds the app is left alone after the user regains control. */
		const RECOVERY_GRACE_MS = 8e3;
		/** Milliseconds a panic buys before the plugin resumes on its own. */
		const PANIC_GRACE_MS = 3e5;
		/** How a hitch should express each of its symptoms. */
		function blocksFor(kinds, mouseMode) {
			return kinds.some((kind) => kind !== "mouse") || kinds.includes("mouse") && mouseMode === "hitch";
		}
		/**
		* The engine.
		*
		* Construct, then `start()`. Register it with `ctx.effect` so `dispose()` runs
		* on unload — an undisposed engine is a page with a frozen transcript row and no
		* way back.
		*/
		var AceEngine = class {
			#settings;
			#waiter;
			#timers;
			#now;
			#registry = createRegistry();
			#budget;
			#schedulers = [];
			#rng;
			#state = "disabled";
			#scanning = 0;
			#heavy = false;
			#hitching = false;
			#graceUntil = 0;
			/** While `now < #pausedUntil`, nothing may block. Distinct from the grace. */
			#pausedUntil = 0;
			#cancelPause;
			#lastSuppression = null;
			#hitchTimes = [];
			/**
			* Blocked milliseconds this session, accumulated here rather than read back
			* out of the Performance API — see `performHitch` for why that would stall at
			* 250 entries and under-report the one number the plugin owes the user.
			*/
			#measuredMs = 0;
			#liveStall;
			#liveChunk;
			#inputLag;
			#cursorLag;
			#unsubscribe = [];
			#removeStyles;
			#listeners = /* @__PURE__ */ new Set();
			/**
			* @param options - settings store and injectable collaborators.
			*/
			constructor(options) {
				this.#settings = options.settings;
				this.#waiter = options.waiter ?? spinWait;
				this.#timers = options.timers ?? realTimers;
				this.#now = options.now ?? (() => performance.now());
				this.#budget = new RollingBudget(effectivePreset(this.#settings.get()).budgetMsPerMinute);
				this.#rng = streamFor(this.#settings.get().seed, "scan");
				this.#inputLag = createInputLag(this.#timers, () => Date.now());
				this.#cursorLag = createCursorLag(this.#timers, this.#registry);
			}
			/** Current status, safe to call at any time. */
			getStatus() {
				const preset = effectivePreset(this.#settings.get());
				const minuteAgo = this.#now() - 6e4;
				this.#hitchTimes = this.#hitchTimes.filter((at) => at > minuteAgo);
				return {
					state: this.#state,
					scanning: this.#scanning,
					heavy: this.#heavy,
					hitchesThisMinute: this.#hitchTimes.length,
					budgetUsedMs: this.#budget.usedMs(this.#now()),
					budgetCapacityMs: preset.budgetMsPerMinute,
					measuredMs: this.#measuredMs,
					lastSuppression: this.#lastSuppression,
					frozen: this.#liveStall?.active === true || this.#liveChunk?.active === true || this.#hitching,
					paused: this.#now() < this.#pausedUntil
				};
			}
			/**
			* Observe status changes.
			* @param listener - called after any state change.
			* @returns an unsubscribe function.
			*/
			subscribe(listener) {
				this.#listeners.add(listener);
				return () => this.#listeners.delete(listener);
			}
			/** Mount everything and begin scheduling. */
			start() {
				if (this.#state === "disposed") return;
				this.#removeStyles = ensureStyles();
				this.#unsubscribe.push(watchArrivals(this.#timers, {
					onArrival: (node, kind) => this.#onArrival(node, kind),
					onConversationSwitch: () => this.#onConversationSwitch()
				}));
				const onVisibility = () => {
					if (document.hidden) this.suspend("hidden");
					else this.resume(RECOVERY_GRACE_MS);
				};
				document.addEventListener("visibilitychange", onVisibility);
				this.#unsubscribe.push(() => document.removeEventListener("visibilitychange", onVisibility));
				this.#unsubscribe.push(this.#settings.subscribe(() => this.#applySettings()));
				this.#grantGrace(RECOVERY_GRACE_MS);
				this.#applySettings();
				this.#notify();
			}
			/**
			* Stop scheduling and release everything currently frozen.
			*
			* Releases, not merely stops: a panic that leaves the transcript row you are
			* staring at pinned is indistinguishable from a hang.
			*
			* @param reason - reported to the HUD.
			*/
			suspend(reason) {
				if (this.#state === "disposed") return;
				this.#lastSuppression = reason;
				for (const scheduler of this.#schedulers) scheduler.suspend(reason);
				this.#releaseAll();
				this.#inputLag.disarm();
				this.#state = reason === "disabled" ? "disabled" : "suspended";
				this.#notify();
			}
			/**
			* Hold the plugin down for a fixed stretch, and say so.
			*
			* Distinct from {@link suspend}, which is a state the caller is responsible
			* for leaving, and distinct from the grace period, which is invisible by
			* design. A panic needs to be *visible* — the user pressed a key and is
			* waiting to see whether it worked — so it keeps a deadline the status
			* reports, and it lifts itself when the deadline passes rather than leaving
			* the plugin silently half-off.
			*
			* @param ms - how long to stay down.
			*/
			pause(ms) {
				if (this.#state === "disposed") return;
				this.#pausedUntil = this.#now() + ms;
				this.suspend("paused");
				this.#cancelPause?.();
				this.#cancelPause = this.#timers.after(ms, () => {
					this.#cancelPause = void 0;
					this.#pausedUntil = 0;
					this.resume(0);
				});
				this.#notify();
			}
			/**
			* Resume after a suspension.
			* @param graceMs - quiet time before the next scan.
			*/
			resume(graceMs) {
				if (this.#state === "disposed") return;
				if (!this.#settings.get().enabled) return;
				this.#grantGrace(graceMs);
				this.#state = "idle";
				for (const scheduler of this.#schedulers) scheduler.resume(graceMs);
				this.#notify();
			}
			/**
			* End an active panic now.
			*
			* Called by the console's `enable()` and by "scan now" — both of which are
			* deliberate requests, unlike a tab regaining focus.
			*
			* @param graceMs - quiet time before scheduling restarts.
			*/
			clearPause(graceMs = RECOVERY_GRACE_MS) {
				if (this.#state === "disposed") return;
				this.#cancelPause?.();
				this.#cancelPause = void 0;
				this.#pausedUntil = 0;
				if (this.#state === "suspended") this.resume(graceMs);
				else this.#notify();
			}
			/**
			* Run one scan immediately.
			*
			* This is what makes the plugin demonstrable — a reviewer should not have to
			* wait out a quiet gap to see whether it works. Routed through the same
			* schedulers, so every escape hatch still applies: pressing it with the
			* settings page open correctly does nothing.
			*/
			scanNow() {
				if (this.#state === "disposed" || !this.#settings.get().enabled) return;
				this.clearPause(0);
				this.#grantGrace(0);
				for (const scheduler of this.#schedulers) scheduler.scanNow();
			}
			/**
			* Tear everything down.
			*
			* Idempotent, and ordered so that nothing can be left mutated: timers and
			* observers first (so nothing new starts), then releases (so nothing stays
			* frozen), then the undo log, then the stylesheet and listeners.
			*
			* Honest limitation: a `dispose()` arriving *during* a synchronous block runs
			* at the next macrotask, up to one block later. Every symptom's backstop timer
			* bounds the damage in the meantime, which is why those exist.
			*/
			dispose() {
				if (this.#state === "disposed") return;
				for (const scheduler of this.#schedulers) scheduler.dispose();
				this.#schedulers = [];
				this.#cancelPause?.();
				this.#cancelPause = void 0;
				this.#releaseAll();
				this.#inputLag.dispose();
				this.#cursorLag.dispose();
				for (const off of this.#unsubscribe) off();
				this.#unsubscribe = [];
				this.#registry.restoreAll();
				this.#removeStyles?.();
				this.#removeStyles = void 0;
				document.documentElement.removeAttribute("data-dsh-age-hitching");
				document.documentElement.removeAttribute("data-dsh-age-freeze-anim");
				this.#state = "disposed";
				this.#notify();
			}
			/** Admit or refuse one planned hitch. The single choke point. */
			#admit = (request) => {
				const settings = this.#settings.get();
				if (this.#state === "disposed") return {
					grantedMs: 0,
					reason: "disposed"
				};
				if (!settings.enabled) return {
					grantedMs: 0,
					reason: "disabled"
				};
				if (settingsPageOpen()) return {
					grantedMs: 0,
					reason: "settings-open"
				};
				if (typeof document !== "undefined" && document.hidden) return {
					grantedMs: 0,
					reason: "hidden"
				};
				if (this.#now() < this.#pausedUntil) return {
					grantedMs: 0,
					reason: "paused"
				};
				if (this.#now() < this.#graceUntil) return {
					grantedMs: 0,
					reason: "grace"
				};
				if (this.#hitching) return {
					grantedMs: 0,
					reason: "hitching"
				};
				const granted = admitByBudget(this.#budget, this.#now(), request, false);
				return granted < 200 ? {
					grantedMs: 0,
					reason: "budget"
				} : { grantedMs: granted };
			};
			/** The scheduler's view of the engine. */
			#host = () => ({
				now: () => this.#now(),
				schedule: (fn, ms) => this.#timers.after(ms, fn),
				admit: this.#admit,
				emit: (event) => this.#onSchedulerEvent(event)
			});
			/**
			* Rebuild the whole schedule from the current settings.
			*
			* Disposing the outgoing schedulers first is load-bearing: they hold live
			* timers, and a rebuild that only replaced the array would leave the previous
			* generation firing scans forever. That is the failure mode where changing a
			* preset makes the plugin *more* aggressive instead of less.
			*/
			#applySettings() {
				const settings = this.#settings.get();
				const preset = effectivePreset(settings);
				for (const scheduler of this.#schedulers) scheduler.dispose();
				this.#schedulers = [];
				this.#scanning = 0;
				this.#heavy = false;
				this.#budget = new RollingBudget(preset.budgetMsPerMinute);
				this.#rng = streamFor(settings.seed, "scan");
				if (!settings.enabled || preset.kinds.length === 0) {
					this.#releaseAll();
					this.#state = "disabled";
					this.#notify();
					return;
				}
				const groups = settings.correlate ? [preset.kinds] : preset.kinds.map((kind) => [kind]);
				this.#schedulers = groups.map((kinds, index) => new AceScheduler(preset, streamFor(settings.seed, index === 0 ? "scan" : kinds[0]), this.#host(), kinds));
				const grace = Math.max(0, this.#graceUntil - this.#now());
				for (const scheduler of this.#schedulers) scheduler.start(grace);
				this.#state = "idle";
				this.#notify();
			}
			#grantGrace(ms) {
				this.#graceUntil = this.#now() + ms;
			}
			/** React to one scheduler event. */
			#onSchedulerEvent(event) {
				switch (event.type) {
					case "scanStart":
						this.#scanning += 1;
						this.#heavy ||= event.heavy;
						this.#state = "scanning";
						this.#notify();
						return;
					case "scanEnd":
						this.#scanning = Math.max(0, this.#scanning - 1);
						if (this.#scanning === 0) {
							this.#heavy = false;
							if (this.#state === "scanning") this.#state = "idle";
						}
						this.#notify();
						return;
					case "hitch":
						this.#runHitch(event.durationMs, event.kinds);
						return;
					case "suppressed":
						this.#lastSuppression = event.reason;
						this.#notify();
						return;
					case "state":
						if (event.state === "hitching" && !this.#hitching) this.#state = "hitching";
						this.#notify();
						return;
					default: return;
				}
			}
			/**
			* Express one admitted hitch.
			*
			* Ordering is deliberate: the DOM symptoms arm *before* the block, because
			* nothing can run during it. Arming afterwards would mean the transcript only
			* freezes once the freeze is already over.
			*/
			#runHitch(durationMs, kinds) {
				const settings = this.#settings.get();
				const preset = effectivePreset(settings);
				if (kinds.length === 0) return;
				this.#hitching = true;
				this.#hitchTimes.push(this.#now());
				const blocking = blocksFor(kinds, settings.mouseMode);
				const drew = (band) => this.#rng.range(band);
				if (kinds.includes("stream-stall")) {
					this.#liveStall?.release();
					this.#liveStall = stallStreamRow(this.#registry, this.#timers, Math.min(drew(preset.streamFreezeMs), durationMs));
				}
				if (kinds.includes("stream-chunk") && !blocking) {
					this.#liveChunk?.stop();
					this.#liveChunk = runChunkGate(this.#registry, this.#timers, this.#waiter, this.#rng.int(preset.chunkFrames[0], preset.chunkFrames[1]), preset.chunkFrameBlockMs);
				}
				if (kinds.includes("stream-stretch")) stretchStreamRow(this.#registry, this.#timers, preset.stretchScale, preset.stretchMode);
				if (kinds.includes("mouse") && settings.mouseMode === "input-lag") this.#inputLag.arm(Math.min(drew(preset.inputLagMs), MAX_CONTINUOUS_BLOCK_MS));
				if (kinds.includes("mouse") && settings.mouseMode === "cursor") this.#cursorLag.freeze();
				if (blocking) {
					document.documentElement.dataset["dshAgeHitching"] = "true";
					if (preset.pauseAnimationsDuringHitch) document.documentElement.dataset["dshAgeFreezeAnim"] = "true";
					try {
						this.#measuredMs += performHitch(this.#waiter, durationMs);
					} finally {
						delete document.documentElement.dataset["dshAgeHitching"];
						delete document.documentElement.dataset["dshAgeFreezeAnim"];
					}
				}
				if (kinds.includes("mouse") && settings.mouseMode === "cursor") this.#cursorLag.settle(durationMs);
				this.#inputLag.disarm();
				this.#hitching = false;
				this.#notify();
			}
			/** Delay a dialog's entrance, if a scan is in progress. */
			#onArrival(node, kind) {
				const settings = this.#settings.get();
				if (!settings.enabled || !settings.interaction) return;
				if (!this.#inScan()) return;
				const preset = effectivePreset(settings);
				const full = this.#rng.range(preset.interactionStallMs);
				stallEntrance(this.#registry, this.#timers, node, kind === "menu" ? full * .5 : full);
			}
			/** A conversation switch is an interaction too, and stalls like one. */
			#onConversationSwitch() {
				const settings = this.#settings.get();
				if (!settings.interaction) return;
				if (!this.#inScan()) return;
				const preset = effectivePreset(settings);
				const stall = this.#rng.range(preset.interactionStallMs);
				const outcome = this.#admit({
					durationMs: stall,
					kinds: ["interaction"],
					heavy: false
				});
				if (outcome.grantedMs < 200) return;
				document.documentElement.dataset["dshAgeHitching"] = "true";
				if (preset.pauseAnimationsDuringHitch) document.documentElement.dataset["dshAgeFreezeAnim"] = "true";
				this.#measuredMs += performHitch(this.#waiter, outcome.grantedMs);
				delete document.documentElement.dataset["dshAgeHitching"];
				delete document.documentElement.dataset["dshAgeFreezeAnim"];
			}
			/** Whether a scan window is open, which is when interactions stall. */
			#inScan() {
				return this.#scanning > 0 || this.#hitching;
			}
			/** Release every live symptom without stopping the schedule. */
			#releaseAll() {
				this.#liveStall?.release();
				this.#liveStall = void 0;
				this.#liveChunk?.stop();
				this.#liveChunk = void 0;
				this.#cursorLag.restore();
				this.#registry.restoreAll();
				document.documentElement.removeAttribute("data-dsh-age-hitching");
				document.documentElement.removeAttribute("data-dsh-age-freeze-anim");
				document.documentElement.removeAttribute("data-dsh-age-cursor-hidden");
			}
			#notify() {
				for (const listener of this.#listeners) listener();
			}
			/** Scheduler states, for tests and diagnostics. */
			get schedulerStates() {
				return this.#schedulers.map((scheduler) => scheduler.state);
			}
			/** The preset currently in force, with the user's symptom switches applied. */
			get preset() {
				return effectivePreset(this.#settings.get());
			}
		};
		//#endregion
		//#region src/client/panic.ts
		/** The primary panic chord. */
		const PANIC_CODE = "KeyA";
		/** Escapes needed to trip the alternate chord. */
		const ESCAPE_COUNT = 3;
		/** Window within which that many escapes count as a panic. */
		const ESCAPE_WINDOW_MS = 1200;
		/** Announcements, in both shipped languages. */
		const COPY = {
			panic: {
				zh: "AGE 安全组件已暂停 5 分钟。再按一次 Ctrl+Alt+Shift+A 可彻底关闭。",
				en: "AGE paused for 5 minutes. Press Ctrl+Alt+Shift+A again to turn it off."
			},
			disabled: {
				zh: "AGE 安全组件已关闭。控制台执行 dshAge.enable() 可重新启用。",
				en: "AGE disabled. Run dshAge.enable() in the console to restore it."
			},
			enabled: {
				zh: "AGE 安全组件已重新启用。",
				en: "AGE re-enabled."
			},
			unloaded: {
				zh: "AGE 已卸载，设置不再生效。",
				en: "AGE is unloaded; its settings no longer apply."
			}
		};
		/**
		* Install the keyboard exits and build the programmatic ones.
		*
		* @param wiring - engine, settings, and where to announce.
		* @returns the exits.
		*/
		function installEscapeHatches(wiring) {
			const { engine, settings, toasts } = wiring;
			const say = (message) => toasts.push(pick(wiring.locale(), message));
			let paused = false;
			const disable = () => {
				paused = false;
				settings.set({ enabled: false });
				engine.suspend("disabled");
				say(COPY.disabled);
			};
			const enable = () => {
				paused = false;
				settings.set({ enabled: true });
				engine.clearPause(RECOVERY_GRACE_MS);
				engine.resume(RECOVERY_GRACE_MS);
				say(COPY.enabled);
			};
			const panic = () => {
				if (paused) {
					disable();
					return;
				}
				paused = true;
				engine.pause(PANIC_GRACE_MS);
				say(COPY.panic);
			};
			let escapes = [];
			const onKeyDown = (event) => {
				if (event.code === PANIC_CODE && event.ctrlKey && event.altKey && event.shiftKey) {
					panic();
					return;
				}
				if (event.key !== "Escape") return;
				const now = Date.now();
				escapes = [...escapes.filter((at) => now - at < ESCAPE_WINDOW_MS), now];
				if (escapes.length < ESCAPE_COUNT) return;
				escapes = [];
				panic();
			};
			window.addEventListener("keydown", onKeyDown, { capture: true });
			return {
				panic,
				disable,
				enable,
				get paused() {
					return paused;
				},
				dispose: () => window.removeEventListener("keydown", onKeyDown, { capture: true })
			};
		}
		/**
		* Install `window.dshAge`.
		*
		* The handle is installed **unconditionally**, including on a load where the
		* plugin is already disabled — otherwise `dshAge.enable()` would be unreachable
		* from exactly the state it exists to fix, which is the same trap as a settings
		* page that cannot be opened.
		*
		* @param wiring - engine, settings, and where to announce.
		* @param version - the package version to report.
		* @returns a disposer that swaps the handle for an inert one.
		*/
		function installConsoleApi(wiring, version) {
			const { engine, settings, toasts } = wiring;
			const say = (message) => toasts.push(pick(wiring.locale(), message));
			const escapes = installEscapeHatches(wiring);
			window.dshAge = {
				version,
				status: () => ({
					enabled: settings.get().enabled,
					preset: settings.get().presetId,
					correlate: settings.get().correlate,
					...engine.getStatus()
				}),
				disable: escapes.disable,
				enable: escapes.enable,
				panic: escapes.panic,
				preset: (id) => settings.set({ presetId: id }),
				hud: (on) => settings.set({ hud: on }),
				hitchesThisMinute: () => engine.getStatus().hitchesThisMinute
			};
			return () => {
				escapes.dispose();
				const gone = () => say(COPY.unloaded);
				window.dshAge = {
					version,
					status: () => ({ unloaded: true }),
					disable: gone,
					enable: gone,
					panic: gone,
					preset: gone,
					hud: gone,
					hitchesThisMinute: () => 0
				};
			};
		}
		//#endregion
		//#region src/client/toasts.ts
		/**
		* The plugin's announcement queue.
		*
		* A real security component tells you it is working, and the announcement is
		* most of what sells the bit — a silent scan is just a stutter. Toasts are also
		* how the escape hatches explain themselves: "paused for 5 minutes" is the
		* difference between a panic key that works and a panic key the user is not sure
		* worked.
		*
		* Deliberately capped and self-expiring. An unbounded queue would leave the
		* corner of the screen muttering about scans long after the user turned the
		* plugin off.
		*/
		/** How long one message stays on screen. */
		const TOAST_TTL_MS = 2400;
		/**
		* Create a queue.
		*
		* @param afterMs - expiry timer; injected so specs need not wait.
		* @returns the store.
		*/
		function createToastStore(afterMs) {
			let toasts = [];
			let nextId = 1;
			const listeners = /* @__PURE__ */ new Set();
			const notify = () => {
				for (const listener of listeners) listener();
			};
			return {
				get: () => toasts,
				push: (text) => {
					const toast = {
						id: nextId += 1,
						text
					};
					toasts = [...toasts, toast].slice(-2);
					notify();
					afterMs(TOAST_TTL_MS, () => {
						toasts = toasts.filter((candidate) => candidate.id !== toast.id);
						notify();
					});
				},
				clear: () => {
					if (toasts.length === 0) return;
					toasts = [];
					notify();
				},
				subscribe: (listener) => {
					listeners.add(listener);
					return () => listeners.delete(listener);
				}
			};
		}
		//#endregion
		//#region src/client/index.tsx
		/**
		* dsh-age, browser half: three registrations and one effect.
		*
		* - `shell.overlay` gets the corner badge. A list slot, additive, and
		*   click-through unless an entry opts in — which is exactly what a HUD needs.
		* - `settings.section` gets the control panel, which is also one of the four
		*   exits from the plugin.
		* - `ctx.effect` owns the engine, and everything the plugin does to the page
		*   happens inside its lifetime. It is registered **last**, so it is the last
		*   thing set up and — because cordis disposes a fiber's effects in reverse —
		*   the first thing torn down.
		*
		* The store and the engine are created here rather than inside a component
		* because the engine is not a React thing: it schedules, blocks and restores on
		* its own, and the React tree only renders what it reports.
		*/
		const name = "dsh-age";
		const inject = ["slots", "locale"];
		/** Reported by `window.dshAge.version`. Keep in step with `package.json`. */
		const VERSION = "0.1.0";
		/** Copy for the scan announcements. */
		const SCAN_COPY = {
			routine: {
				zh: "AGE 安全组件正在扫描…",
				en: "AGE is running a scan…"
			},
			heavy: {
				zh: "AGE 安全组件正在初始化并执行运行环境扫描，可能造成短暂卡顿。",
				en: "AGE is initialising and scanning the runtime environment. Brief stutter is expected."
			}
		};
		/**
		* Register both surfaces and start the engine.
		*
		* @param ctx - client root context.
		*/
		function apply(ctx) {
			const settings = createAgeSettingsStore();
			const toasts = createToastStore((ms, fn) => {
				const handle = setTimeout(fn, ms);
				return () => clearTimeout(handle);
			});
			const engine = new AceEngine({ settings });
			const locale = () => ageLocale(ctx.locale.getSnapshot().active);
			const set = (patch) => settings.set(patch);
			/**
			* The HUD entry.
			*
			* `shell.overlay` is click-through by default, but its layer carries a `> *`
			* rule that hands pointer events back to every direct child — so the badge
			* itself is responsible for staying out of the way, which `AgeHud` does.
			*
			* @returns the corner badge, or nothing when the user has hidden it.
			*/
			function AgeHudEntry(_props) {
				const current = useAgeSettings(settings);
				const [status, setStatus] = (0, react.useState)(() => engine.getStatus());
				const [visibleToasts, setVisibleToasts] = (0, react.useState)(() => toasts.get());
				const activeLocale = locale();
				(0, react.useEffect)(() => engine.subscribe(() => setStatus(engine.getStatus())), []);
				(0, react.useEffect)(() => toasts.subscribe(() => setVisibleToasts(toasts.get())), []);
				if (!current.hud) return null;
				return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AgeHud, {
					status,
					settings: current,
					toasts: visibleToasts,
					locale: activeLocale,
					onPanic: () => window.dshAge?.panic(),
					onDisable: () => window.dshAge?.disable(),
					onPreset: (id) => set({ presetId: id }),
					onToggleCollapsed: () => set({ hudCollapsed: !current.hudCollapsed })
				});
			}
			/**
			* The settings entry.
			* @returns the AGE control panel.
			*/
			function AgeSectionEntry(_props) {
				const current = useAgeSettings(settings);
				const [status, setStatus] = (0, react.useState)(() => engine.getStatus());
				const activeLocale = locale();
				(0, react.useEffect)(() => engine.subscribe(() => setStatus(engine.getStatus())), []);
				(0, react.useEffect)(() => () => engine.resume(RECOVERY_GRACE_MS), []);
				return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AgeSection, {
					settings: current,
					status,
					locale: activeLocale,
					onSet: set,
					onScanNow: () => engine.scanNow(),
					onPanic: () => window.dshAge?.panic(),
					onDisable: () => window.dshAge?.disable(),
					onEnable: () => window.dshAge?.enable(),
					onReset: () => settings.reset()
				});
			}
			ctx.slots.inject("shell.overlay", () => ctx.slots.register({
				name: "shell.overlay",
				id: "dsh-age-hud",
				order: 60
			}, AgeHudEntry));
			ctx.slots.inject("settings.section", () => ctx.slots.register({
				name: "settings.section",
				id: "dsh-age",
				order: 95,
				label: () => pick(locale(), {
					zh: "AGE 安全组件（恶搞）",
					en: "AGE Anti-Cheat (Parody)"
				})
			}, AgeSectionEntry));
			ctx.effect(() => {
				const consoleDisposer = installConsoleApi({
					engine,
					settings,
					toasts,
					locale
				}, VERSION);
				let previousScans = 0;
				const offStatus = engine.subscribe(() => {
					const status = engine.getStatus();
					if (status.scanning > previousScans) {
						if (status.heavy) toasts.push(pick(locale(), SCAN_COPY.heavy));
						else if (Math.random() * 100 < settings.get().toastChance) toasts.push(pick(locale(), SCAN_COPY.routine));
					}
					previousScans = status.scanning;
				});
				engine.start();
				return () => {
					offStatus();
					consoleDisposer();
					toasts.clear();
					engine.dispose();
				};
			}, "dsh-age: engine");
		}
		//#endregion
		exports.apply = apply;
		exports.inject = inject;
		exports.name = name;
		return module.exports;
	}
});
